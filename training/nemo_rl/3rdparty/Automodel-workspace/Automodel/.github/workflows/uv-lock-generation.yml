# Copyright (c) 2025, NVIDIA CORPORATION. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Generate Uv lock

on:
  pull_request:
    paths:
      - 'pyproject.toml'

jobs:
  check_uv_lock_and_update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          token: ${{ secrets.PAT }}

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"
      - name: Set up UV
        uses: astral-sh/setup-uv@v1
        with:
          version: 0.8.22

      - name: Check Uv lock
        run: |
          set +e
          uv lock --check
          EXIT_CODE=$?
          if [ $EXIT_CODE -eq 0 ]; then
            echo 'Uv lock file is update to date'
          else
            echo 'Updating uv lock file'
            uv lock
          fi

      - name: Check Uv PyTorch lock
        run: |
          set +e
          mv uv.lock uv_main.lock
          mv docker/common/uv-pytorch.lock uv.lock
          sed -i '/\[tool\.uv\]/r docker/common/uv-pytorch.toml' pyproject.toml
          uv lock --check
          EXIT_CODE=$?
          if [ $EXIT_CODE -eq 0 ]; then
            echo 'Uv PyTorch lock file is update to date'
          else
            echo 'Updating uv PyTorch lock file'
            uv lock
          fi
          mv uv.lock docker/common/uv-pytorch.lock
          mv uv_main.lock uv.lock

      - name: Commit and push uv lock updates
        run: |
          git config --global user.email "nemo-bot@nvidia.com"
          git config --global user.name "NeMo Bot"

          if git diff --quiet uv.lock docker/common/uv-pytorch.lock; then
            echo "No changes to commit"
          else
            git add uv.lock docker/common/uv-pytorch.lock
            git commit --signoff -m "Update uv lock"
            git push
          fi
