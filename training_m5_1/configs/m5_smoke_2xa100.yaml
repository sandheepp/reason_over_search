# M5.1 smoke config — 2x A100-80GB variant.
#
# Purpose: verify the 2-GPU shape (DTensor TP=2, vLLM TP=2, gpus_per_node=2)
#          end-to-end before any prod 2xa100 sbatch ships. Mirrors m5_smoke.yaml
#          in every paper-tagged knob; only the multi-GPU + ckpt + memory knobs
#          differ. With max_num_steps=4 + save_period=2, this produces 2
#          checkpoints (steps 2, 4) on 2x A100 in ~10-15 min.
#
# Differences from m5_smoke.yaml (1x):
#   - cluster.gpus_per_node: 1 -> 2
#   - policy.dtensor_cfg.tensor_parallel_size: 1 -> 2  (training partitioned)
#   - policy.generation.vllm_cfg.tensor_parallel_size: 1 -> 2  (rollout partitioned)
#   - policy.generation.vllm_cfg.gpu_memory_utilization: 0.5 -> 0.7  (TP=2 frees mem)
#   - wandb run name: m5_smoke -> m5_smoke_2xa100
#
# Run from repo root inside a 2-A100 srun (gpu-short or gpu-a100-80g):
#   bash training_m5_1/scripts/run.sh --mode smoke_2xa100
#
# Companion: m5_1_research_paper_2xa100.yaml (the production 2xa100 config).

# =============================================================================
# GRPO algorithm
# =============================================================================
grpo:
  num_generations_per_prompt: 5
  num_prompts_per_step: 4
  max_num_steps: 4
  max_num_epochs: 1
  val_period: 0
  val_at_start: false
  val_at_end: false
  max_val_samples: 256
  val_batch_size: 256
  max_rollout_turns: 10
  normalize_rewards: true
  use_leave_one_out_baseline: false
  overlong_filtering: false
  use_dynamic_sampling: false
  dynamic_sampling_max_gen_batches: 10
  batch_multiplier: 1
  reward_shaping:
    enabled: false
    overlong_buffer_length: 128
    overlong_buffer_penalty: 1
    max_response_length: ${policy.max_total_sequence_length}
    stop_properly_penalty_coef: null
  adv_estimator:
    name: "grpo"
    normalize_rewards: ${grpo.normalize_rewards}
    use_leave_one_out_baseline: ${grpo.use_leave_one_out_baseline}
    minus_baseline: true
  reward_scaling:
    enabled: false
    source_min: 0.0
    source_max: 1.0
    target_min: 0.0
    target_max: 1.0
  seq_logprob_error_threshold: null
  seed: 42
  async_grpo:
    enabled: false
    max_trajectory_age_steps: 1
    in_flight_weight_updates: false
    recompute_kv_cache_after_weight_updates: false

# =============================================================================
# Loss
# =============================================================================
loss_fn:
  reference_policy_kl_penalty: 0.001
  reference_policy_kl_type: "k3"
  kl_input_clamp_value: 20.0
  kl_output_clamp_value: 10.0
  ratio_clip_min: 0.2
  ratio_clip_max: 0.2
  ratio_clip_c: null
  use_on_policy_kl_approximation: false
  use_importance_sampling_correction: false
  truncated_importance_sampling_ratio: null
  truncated_importance_sampling_ratio_min: null
  truncated_importance_sampling_type: tis
  sequence_level_importance_ratios: false
  token_level_loss: true
  force_on_policy_ratio: false
  use_kl_in_reward: false

# =============================================================================
# Checkpointing — ENABLED (mirrors prod save shape; verifies 2-GPU ckpt path)
# =============================================================================
checkpointing:
  enabled: true
  checkpoint_dir: "results/grpo/m5_smoke_2xa100"
  metric_name: null
  higher_is_better: false
  keep_top_k: null
  save_period: 2
  checkpoint_must_save_by: null
  model_save_format: "safetensors"
  save_consolidated: true
  save_optimizer: false

# =============================================================================
# Policy (model + training + generation)
# =============================================================================
policy:
  model_name: "Qwen/Qwen3.5-0.8B"
  tokenizer:
    name: ${policy.model_name}
    chat_template_kwargs: null
  hf_config_overrides: {}

  train_global_batch_size: 20
  train_micro_batch_size: 2
  generation_batch_size: 32
  logprob_batch_size: ${policy.train_micro_batch_size}
  max_total_sequence_length: 4096
  precision: "bfloat16"
  logprob_chunk_size: null
  offload_optimizer_for_logprob: false

  # ---- DTensor backend ----
  dtensor_cfg:
    _v2: true
    enabled: true
    cpu_offload: false
    sequence_parallel: false
    activation_checkpointing: true
    # TP=2 partitions the model across both GPUs — the load-bearing 2x
    # speedup knob (matches m5_1_research_paper_2xa100.yaml's training side).
    tensor_parallel_size: 2  # Option A: TP=2 with custom_parallel_plan (Qwen3.5 vocab-parallel FSDP bug workaround)
    context_parallel_size: 1
    custom_parallel_plan: training_m5_1.src.parallel_plan_qwen35.custom_parallel_plan  # Option A: Qwen3.5 vocab-parallel FSDP bug workaround
    automodel_kwargs: {force_hf: true}  # NeMo-RL issue 2072: bypass automodel state_dict adapter, fixes TP=2 vocab-parallel lm_head refit
    lora_cfg:
      enabled: false
      target_modules: []
      exclude_modules: []
      match_all_linear: true
      dim: 8
      alpha: 32
      dropout: 0.0
      dropout_position: "post"
      lora_A_init: "xavier"
      use_triton: true

  # ---- Megatron backend off ----
  megatron_cfg:
    enabled: false
    force_reconvert_from_hf: false
    empty_unused_memory_level: 1
    activation_checkpointing: false
    converter_type: "Qwen2ForCausalLM"
    tensor_model_parallel_size: 1
    expert_tensor_parallel_size: 1
    expert_model_parallel_size: 1
    pipeline_model_parallel_size: 1
    num_layers_in_first_pipeline_stage: null
    num_layers_in_last_pipeline_stage: null
    context_parallel_size: 1
    pipeline_dtype: ${policy.precision}
    sequence_parallel: false
    freeze_moe_router: true
    moe_router_dtype: "fp64"
    moe_router_load_balancing_type: "none"
    moe_router_bias_update_rate: 0.0
    moe_permute_fusion: false
    apply_rope_fusion: true
    bias_activation_fusion: true
    defer_fp32_logits: false
    moe_per_layer_logging: false
    moe_enable_deepep: false
    moe_token_dispatcher_type: "alltoall"
    moe_shared_expert_overlap: false
    gradient_accumulation_fusion: false
    peft:
      enabled: false
      target_modules: []
      exclude_modules: []
      dim: 8
      alpha: 32
      dropout: 0.0
      dropout_position: "post"
      lora_A_init_method: "xavier"
      lora_B_init_method: "zero"
      a2a_experimental: false
      lora_dtype: None
    optimizer:
      optimizer: "adam"
      lr: 1.0e-6
      min_lr: 1.0e-7
      weight_decay: 0.01
      bf16: true
      fp16: false
      params_dtype: "float32"
      adam_beta1: 0.9
      adam_beta2: 0.999
      adam_eps: 1e-8
      sgd_momentum: 0.9
      use_distributed_optimizer: true
      use_precision_aware_optimizer: true
      clip_grad: ${policy.max_grad_norm}
      optimizer_cpu_offload: false
      optimizer_offload_fraction: 0.0
    scheduler:
      start_weight_decay: ${policy.megatron_cfg.optimizer.weight_decay}
      end_weight_decay: ${policy.megatron_cfg.optimizer.weight_decay}
      weight_decay_incr_style: "constant"
      lr_decay_style: "constant"
      lr_decay_iters: 1000
      lr_warmup_iters: 14
      lr_warmup_init: 1.0e-7
    distributed_data_parallel_config:
      grad_reduce_in_fp32: false
      overlap_grad_reduce: true
      overlap_param_gather: true
      use_custom_fsdp: false
      data_parallel_sharding_strategy: "optim_grads_params"
    fp8_cfg:
      enabled: false
      fp8: "e4m3"
      fp8_recipe: "blockwise"
      fp8_param: false
    env_vars: null

  draft:
    enabled: false
    model_name: null
    loss_weight: 0.1
    num_layers: null
    aux_layer_indices: null

  dynamic_batching:
    enabled: true
    train_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.train_micro_batch_size}}
    logprob_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.logprob_batch_size}}
    sequence_length_round: 64

  sequence_packing:
    enabled: false
    train_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.train_micro_batch_size}}
    logprob_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.logprob_batch_size}}
    algorithm: "modified_first_fit_decreasing"
    sequence_length_round: 64

  make_sequence_length_divisible_by: ${policy.dtensor_cfg.tensor_parallel_size}
  max_grad_norm: 1.0

  optimizer:
    name: "torch.optim.AdamW"
    kwargs:
      lr: 1.0e-6
      weight_decay: 0.01
      betas: [0.9, 0.999]
      eps: 1e-8
      foreach: false
      fused: false

  scheduler:
    - name: "torch.optim.lr_scheduler.LinearLR"
      kwargs:
        start_factor: 1.0
        end_factor: 1.0
        total_iters: 1
    - name: "torch.optim.lr_scheduler.ConstantLR"
      kwargs:
        factor: 1.0
        total_iters: 10000000000
    - milestones: [1]

  generation:
    backend: "vllm"
    max_new_tokens: 1024
    temperature: 1.0
    top_p: 1.0
    top_k: null
    stop_token_ids: null
    stop_strings: ["</tool_call>", "</search>", "</answer>"]
    mcore_generation_config:
      buffer_size_gb: 10
      num_cuda_graphs: 4
      block_size_tokens: 256
      use_cuda_graphs_for_non_decode_steps: true
      enable_chunked_prefill: true
      unified_memory_level: 0
      max_tokens: 16384
    vllm_cfg:
      async_engine: false
      precision: ${policy.precision}
      kv_cache_dtype: "auto"
      # TP=2 partitions vLLM's rollout weights across both GPUs (matches
      # m5_1_research_paper_2xa100.yaml).
      tensor_parallel_size: 2
      pipeline_parallel_size: 1
      expert_parallel_size: 1
      # 0.7 (was 0.5 on 1x): with TP=2 each GPU holds half the weights, so
      # vLLM's asleep footprint per-GPU is smaller and we can afford a larger
      # KV cache budget. Matches the prod 2xa100 yaml.
      gpu_memory_utilization: 0.7
      max_model_len: ${policy.max_total_sequence_length}
      enforce_eager: false
      use_deep_gemm: false
      num_last_layers_in_bf16: 0
      num_first_layers_in_bf16: 0
      enable_vllm_metrics_logger: true
      vllm_metrics_logger_interval: 0.5
    vllm_kwargs: {}
    colocated:
      enabled: true
      resources:
        gpus_per_node: null
        num_nodes: null

# =============================================================================
# Data — MuSiQue (M5.1 single-dataset training)
# =============================================================================
data:
  max_input_seq_length: ${policy.max_total_sequence_length}
  shuffle: true
  num_workers: 1
  use_multiple_dataloader: false

  train:
    dataset_name: search_r1
    data_path: data/training/musique/train.parquet
    arm: qwen_native
    seed: ${grpo.seed}

  validation: null

  default:
    prompt_file: training_m5_1/src/prompts/m5_qwen35_user.txt
    system_prompt_file: null
    processor: search_r1_processor
    env_name: search_r1

# =============================================================================
# Env — Search-R1 retrieval Ray actor
# =============================================================================
env:
  search_r1:
    arm: qwen_native
    retriever_url: "http://127.0.0.1:3005"
    top_n: 5
    max_turns: 10
    max_obs_chars: 1024
    request_timeout_s: 30.0

# =============================================================================
# Logging
# =============================================================================
logger:
  log_dir: "logs"
  num_val_samples_to_print: 5
  wandb_enabled: true
  tensorboard_enabled: false
  mlflow_enabled: false
  swanlab_enabled: false
  monitor_gpus: true
  wandb:
    project: "reason_over_search_m5_1"
    name: "qwen3.5-0.8b-musique-m5_smoke_2xa100-seed42"
  swanlab:
    project: "reason_over_search_m5_1"
    name: "qwen3.5-0.8b-musique-m5_smoke_2xa100-seed42"
  tensorboard: {}
  mlflow:
    experiment_name: "reason_over_search_m5_1"
    run_name: "qwen3.5-0.8b-musique-m5_smoke_2xa100-seed42"
    tracking_uri: "http://localhost:5000"
  gpu_monitoring:
    collection_interval: 10
    flush_interval: 10

# =============================================================================
# Cluster
# =============================================================================
cluster:
  # 2x A100-80GB on one node.
  gpus_per_node: 2
  num_nodes: 1
