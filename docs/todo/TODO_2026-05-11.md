---
title: TODO 2026-05-11 — M5.1 live + next step (B200 port + optimization)
tags: [todo, catchup, m5, m5.1, b200, portability]
source: internal
created: 2026-05-11
updated: 2026-05-11
---

# TODO 2026-05-11 — M5.1 live + next step (B200 port + optimization)

> **SUPERSEDED 2026-05-12** by [`TODO_2026-05-12.md`](TODO_2026-05-12.md). The "M5.1 live on A100" framing in this doc is no longer accurate — a1 crashed at step 50 and a2 was killed at step 15. Read this doc only for historical context (the B200/H100/Lambda port comparison material here is still useful when a3 is up and stable).

**Audience:** Sanju, Jose, fresh agent session.
**Goal:** Snapshot of where the project is one day after [`TODO_2026-05-10.md`](TODO_2026-05-10.md). The big change: **M5 paper-recipe training (experiment 1) is built, smoked, and live on 1× A100**. Active question now is whether to port the run to B200 on RunPod for ~5× speedup and use the saved wall-clock to start M5.2 ablations sooner.

---

## 30-second story

**M5.1 (Qwen3.5-0.8B GRPO on MuSiQue, ReSearch paper recipe) is running.** Launched 2026-05-11 01:05 UTC; W&B run `qwen3.5-0.8b-musique-m5_prod-seed42`; exp_010 on disk; pid 178441. Live trend at step 17 is **10–11 min/step steady-state → ~4.5 d full run / ~$130 on Vast A100**. Reward is climbing (0.020 → 0.13 peak), gen length is collapsing (7038 → 653 tokens), truncation rate hit 0% by step 15. The recipe works.

**Branch state:** the live training is on `research_v2`. A snapshot of the working state of the experiment-1 (paper-recipe) code + docs is also on `experiment_1` (created today, will not move). Continue work on `research_v2`.

**Next step:** port to **1× B200 on RunPod (~14–16 h, ~$90)** or **1× H100 SXM on Vast (~2 d, ~$90)** to free the A100 sooner. Then start M5.2 system-gain optimization on the same hardware.

---

## What's done since 2026-05-10

| Milestone | Status | Headline |
|---|---|---|
| **M5 scaffold** | closed 2026-05-09 | [`training_m5_1/`](../../training_m5_1/) self-contained scaffold copied from `training/`; 14 import-path renames; reward/parser/template overlays re-export from `evaluation_qwen35` for byte-alignment. [`README`](../../training_m5_1/README.md) |
| **M5 smoke (v1–v6)** | closed 2026-05-10 | 6 iterations, 1 success (v6: 93.1 s/step ex-warmup on 20 traj/step, 10 steps clean). Iteration log in [`RESULTS_SMOKE_m5.md` §1–§2](../report/RESULTS_SMOKE_m5.md) |
| **M5.2 v7 OOM postmortem** | closed 2026-05-10 | Production seq=8192 doubled the log_softmax peak; micro=2 OOM'd by 0.62 GiB. Root cause: NeMo-RL's TP=1 path at `model_utils.py:1378` doesn't chunk the fp32 cast. Fix taken: micro=1 (forced 25 d projection at the time). Patch deferred to M5.3 (would unlock micro=2 → ~halve training wall) |
| **M5.2 system gains (O1, R2)** | folded into prod yaml 2026-05-10 | Bundled commit: O1 fused AdamW + R2 vLLM async_engine. ~1–3% combined per step; both shipped features, no risk. R1 (prefix caching) already on by default on A100 cc≥8 |
| **M5.1 production config** | live since 2026-05-11 01:05 UTC | [`configs/m5_1_research_paper.yaml`](../../training_m5_1/configs/m5_1_research_paper.yaml) (paper-faithful + M5.2 O1+R2); 622 steps × 320 traj/step × 2 epochs; first ckpt at step 50 |
| **Live trajectory** | step 17 (10 min) and counting | Per-step collapsed 58 → 10 min over 17 steps; ETA tightened 25 → 14-16 → 10-13 → **~4.5 d**. Full table in [`RESULTS_SMOKE_m5.md` §6.2](../report/RESULTS_SMOKE_m5.md#62-per-step-trajectory-live-refresh-as-steps-land) |
| **Hardware comparison doc** | v0 archived 2026-05-11; v1 live | [`HARDWARE_COMPARISON.md`](../setup/HARDWARE_COMPARISON.md) anchored on live step-15-17 trend; per-config wall-clock + cost table; v0 (step-8 anchor, superseded 3×) in [`archive/HARDWARE_COMPARISON_v0.md`](../archive/HARDWARE_COMPARISON_v0.md). Includes Vast, RunPod, **Thundercompute** (verified, beta, cheapest A100) |
| **Portability review pass** | closed 2026-05-11 | CLAUDE.md / SETUP_VAST.md / training_m5_1/README.md / CODE_SETUP_m5.md fixed to remove stale M2 references and document the RunPod port path. Audit details in commit `2227291` |
| **Branch `experiment_1`** | created 2026-05-11 | Frozen snapshot of the paper-recipe state at the point M5.1 went live; not moving forward. Active work continues on `research_v2` |

Pending items also in [`TODO.md`](TODO.md) (evergreen).

---

## Active target

**Port to a faster GPU.** ⚠ **Updated PM 2026-05-11**: the per-step trend reversed. The step-17 trough (10.4 min, that drove the AM estimate of ~4.5 d / $130 on A100) was a U-shape minimum, not a plateau. By step 42 it had climbed to **~23.7 min/step** → **~10.2 d total / ~$294 on A100**, ~2.3× the AM estimate.

Reward is also climbing strongly (0.10 → 0.19), so the trend is *good* — model learning to reason longer when needed. But the economics flipped back to "port the run".

| Option | Wall | Cost | Δ vs "let A100 finish" |
|---|---:|---:|---:|
| Let A100 finish | 10.2 d | ~$294 | baseline |
| **2× H100 SXM TP=2 (Vast)** | **1.7 d** | **~$172** | **-$122, save 8.5 d** |
| **1× B200 (RunPod spot)** | **~34 h** | **~$220** | **-$74, save 8.8 d** |
| 1× H100 SXM (Vast) | 4.6 d | ~$225 | -$69, save 5.6 d |

Sunk cost on A100 = $19 (15.7 h to step 42; included in all "switch now" totals above). Full hardware-comparison table + recommendation revision in [`../setup/HARDWARE_COMPARISON.md`](../setup/HARDWARE_COMPARISON.md) (v2; v1 archived at [`../archive/HARDWARE_COMPARISON_v1.md`](../archive/HARDWARE_COMPARISON_v1.md)).

**Recommended sequence**:
1. Wait until step 50 lands (~6-7 more hours on A100, ~$8) → first checkpoint = recovery insurance.
2. Choose the swap target. **2× H100 SXM TP=2 on Vast** is the new Pareto pick (cheapest + ≤2 d); **1× B200 on RunPod** if you want single-GPU and same-day finish.
3. Smoke the target hardware (~30 min, ~$3) to derisk: B200 = Blackwell sm_100 + v2 worker venv compat; 2× H100 = TP=2 byte-exact prompt re-validate.
4. Launch the swap. Kill the A100 once the swap is healthy (don't kill before).

### If "stay on A100"
- Nothing to do; the run is healthy. First ckpt at step 50 (~30 h from start → 2026-05-12 ~07 UTC); finishes around 2026-05-15.
- Use the gap to author M5.2 ablation plan: which non-paper-faithful levers to test on the next training surface (lever menu: [`research/RUNTIME_EFFICIENCY.md`](../research/RUNTIME_EFFICIENCY.md)).

### If "port to B200 on RunPod" (recommended)
B200 instance + a 30-min smoke to derisk the Blackwell sm_100 path before kicking off the multi-day run. Sequence:

1. **Rent 1× B200 on RunPod Secure Cloud** (~$5.98/h spot, ~$7.99/h Secure). Image: `pantomiman/reason-over-search-v1:v2`. Mount: confirm `/workspace` (RunPod default).
2. **Bootstrap**: `cd /workspace && git clone <repo> reason_over_search && cd reason_over_search && git checkout research_v2 && bash training/scripts/bootstrap.sh`. Expected: ~25 min on HF fast path. If bootstrap fails, fall back to `experiment_1` branch (frozen working state).
3. **Compatibility smoke** (~30 min, ~$3): run a v6-equivalent 10-step smoke on the B200 box: `cd training_m5_1 && bash scripts/smoke.sh`. The compat risk is the `DTensorPolicyWorkerV2` venv (mamba-ssm + flash-attn + causal-conv1d + nv-grouped-gemm) compiled against Hopper-era CUDA — sm_100 may need a rebuild. Watch for: kernel-launch errors, `nv-grouped-gemm` CUDA-init failures.
4. **If smoke green**: flip `train_micro_batch_size: 1 → 2` in `m5_1_research_paper.yaml` (192 GB easily fits at seq=8192) and launch `bash training_m5_1/scripts/run.sh --mode prod`. Expected: ~1.4 min/step → ~14–16 h, ~$90.
5. **If smoke red**: fallback to **2× H200 (TP=2)** on RunPod (also ~$80, also ~18 h, no Blackwell risk) — needs `policy.parallelism.tensor_model_parallel_size: 2` yaml flip and the M4 byte-exact prompt re-validation (the chunked-logprobs path comes for free with TP≥2).

Either way the live A100 run can be killed *after* the step-50 checkpoint lands (so you have a recoverable artifact regardless of port outcome).

### Then: M5.2 system gains
Once the M5.1 paper-faithful run lands a final checkpoint, the lever menu opens up. Top candidates from [`research/RUNTIME_EFFICIENCY.md`](../research/RUNTIME_EFFICIENCY.md) (filtered to throughput-only, paper-orthogonal):

- **NeMo-RL `model_utils.py:1378` patch** — chunk the fp32 logit cast on the TP=1 path. Unlocks `train_micro_batch_size: 2` at seq=8192 → ~halves training-phase wall. Source-mod effort + smoke re-validate. Biggest single-lever speedup available.
- **Sequence packing** — turned off because Qwen3.5 GatedDeltaNet crashes with packed sequences ([`training/fix/CHANGES.md`](../../training/fix/CHANGES.md) §5). Patching upstream would re-enable a ~1.5-2× lever. Out-of-scope for one experiment.
- **FP8 / Transformer Engine** — on Hopper+ / Blackwell, would be ~2× on training+logprobs. Substantial port through DTensor; not for this run.
- **Async-GRPO** — touches training math, not paper-orthogonal. Defer.

---

## Lean reading path (~30 min; in order)

| # | File | Why | Time |
|---|------|-----|------|
| 1 | This doc § "Active target" | What to decide today | 3 min |
| 2 | [`setup/HARDWARE_COMPARISON.md`](../setup/HARDWARE_COMPARISON.md) | Per-config wall-clock + cost; provider comparison (Vast / RunPod / Thundercompute) | 10 min |
| 3 | [`report/RESULTS_SMOKE_m5.md` §6](../report/RESULTS_SMOKE_m5.md#6-m51-production-training--live) | Live trajectory, health signals, ETA evolution | 8 min |
| 4 | [`report/CODE_SETUP_m5.md` §3.3, §4](../report/CODE_SETUP_m5.md) | What changed M2 → M5.1 (smoke vs prod knob table; paper-vs-ours hyperparameter table) | 5 min |
| 5 | [`setup/SETUP_INSTANCE.md` § Porting to non-Vast hosts](../setup/SETUP_INSTANCE.md) | The RunPod-specific bootstrap path | 5 min |

Skip the older TODO files unless you need M3 / M3.1 / M4 context.

---

## Open questions for Sanju

1. **B200 port vs stay-on-A100?** $50 to save ~3 days. Net delta is small — does the freed wall-clock matter for M5.2 schedule?
2. **NeMo-RL patch** (`model_utils.py:1378` chunked fp32 cast) — worth pursuing as a source-mod investment? Unlocks micro=2 → ~halves training wall on any GPU, not just B200. Re-validation cost is one smoke per affected experiment.
3. **M5.2 first lever** — when the new training box is up, which lever first? Suggest: prefix-caching is already on; async_engine is already on; the next meaningful lever is `gpu_memory_utilization` rebump (memory headroom on H200/B200 makes 0.5 → 0.7 safe).
4. **Sequence packing investigation** — worth tracing the GatedDeltaNet crash upstream so we can re-enable packing? Big lever, but a rabbit hole.
5. **Eval the in-flight ckpt** — at step 50 the first checkpoint lands; do we eval immediately against M4 floor (hybrid mean EM 0.057) or wait for step 100+ to be sure the trend is real?

---

## Deferred from the portability audit (not blockers)

These came out of the 2026-05-11 portability review but didn't ship in the review commit. Pick them up if/when a port to a new host actually trips on them:

- `training/scripts/bootstrap.sh:50` hardcodes `/workspace` for the disk-free check. Both Vast and RunPod default to `/workspace` so this hasn't bitten; document the override pattern (`HF_HOME=<mount>/hf_cache`, `TMPDIR=<mount>/tmp_build`) in BOOTSTRAP_NEW_INSTANCE.md if a different host is used.
- `bootstrap.sh` lines 102–105 don't pre-flight `git-lfs`. If `git lfs` is missing from a fresh image, the `git lfs pull` is a no-op and the parquet pull fails silently downstream.
- `bootstrap.sh` line 78–88 EVAL_PY fallback (`/opt/miniforge3/envs/evaluation_search_r1/`) is untested on non-Vast hosts. Adding `command -v` style pre-checks would catch image drift early.

---

## Pointers (consolidated)

### Where the project lives right now

- **Live training**: `exp_010` on disk; pid 178441; W&B run `qwen3.5-0.8b-musique-m5_prod-seed42` on project `reason_over_search_m5_1`. Log: `logs/m5_1_full.log`. Killable with `kill 178441` (preserves logs but not training state pre-checkpoint).
- **Experiment 1 frozen snapshot**: branch `experiment_1` (created 2026-05-11; will not move). The exact code+docs state at the moment M5.1 launched.
- **Active branch**: `research_v2` (move forward here).

### Doc map for the port

- [`setup/HARDWARE_COMPARISON.md`](../setup/HARDWARE_COMPARISON.md) — accelerator + provider + cost (live)
- [`setup/HARDWARE_4090.md`](../setup/HARDWARE_4090.md) — historical 4090 dev-box (M0–M1 era)
- [`archive/HARDWARE_COMPARISON_v0.md`](../archive/HARDWARE_COMPARISON_v0.md) — superseded v0 (step-8 anchor)
- [`setup/SETUP_INSTANCE.md`](../setup/SETUP_INSTANCE.md) — canonical bootstrap; § "Porting to non-Vast hosts" covers RunPod
- [`setup/BOOTSTRAP_NEW_INSTANCE.md`](../setup/BOOTSTRAP_NEW_INSTANCE.md) — generic (non-Vast) wrapper
- [`training_m5_1/README.md`](../../training_m5_1/README.md) — experiment-1 launch instructions
- [`milestone_5/MILESTONE_5.md`](../milestone_5/MILESTONE_5.md) — design + decision narrative
- [`milestone_5/PAPER_VS_OURS_M5.md`](../milestone_5/PAPER_VS_OURS_M5.md) — paper-to-yaml mapping (every divergence flagged)
- [`report/CODE_SETUP_m5.md`](../report/CODE_SETUP_m5.md) — code-setup audit (M5 + M5.1; live)
- [`report/RESULTS_SMOKE_m5.md`](../report/RESULTS_SMOKE_m5.md) — smoke + live trajectory log (steps 1–17 tabulated)
- [`research/RUNTIME_EFFICIENCY.md`](../research/RUNTIME_EFFICIENCY.md) — M5.2 lever menu

---

## Backlog (M5.2+ candidates, lower priority)

| # | Item | Owner | Blocked on |
|---|---|---|---|
| 1 | B200 / H100 SXM port + 30-min compat smoke | Sanju | nothing (next thing to do) |
| 2 | M5.1 step-50 checkpoint eval against M4 untrained floor | — | step 50 (~30 h from launch) |
| 3 | NeMo-RL `model_utils.py:1378` chunked fp32-cast patch | — | M5.1 final ckpt landed (so a smoke re-validation has a baseline) |
| 4 | M5.2 lever 1 — `gpu_memory_utilization` rebump on H200/B200 | — | (1) |
| 5 | bootstrap.sh pre-flight checks (git-lfs, EVAL_PY) | — | (1) (test on real RunPod) |
| 6 | Sequence-packing GatedDeltaNet crash investigation | — | (3) |
| 7 | Cross-dataset variant (HotpotQA + 2Wiki + MuSiQue mix) — M5.2 candidate per [`MILESTONE_5.md`](../milestone_5/MILESTONE_5.md) "Parallel experiments" | — | (2) |
