---
title: Log
tags: []
source: internal
created: 2026-05-06
updated: 2026-05-11
---

# Wiki log

Append-only chronological record of ingest events, decisions, and learnings. New entries go on top (newest first). Each day gets a single `## YYYY-MM-DD` heading; entries within a day are bullets.

Format:

```
## YYYY-MM-DD
- Ingested: <source path or URL> -> <pages touched>
- Learned: <one-liner>, filed in <page>
- Decision: <one-liner>, recorded in <page>
- Lint: <summary of lint cycle output and what was fixed>
```

Conventions:

- Cite sources by relative path (e.g. `../raw/2026-05-06_foo.pdf`) or by full URL.
- Cite wiki pages by relative path from this file (e.g. `report/CONVERSATION_CONTEXT.md`).
- Keep each bullet to one line. Synthesis goes in the wiki page, not here.
- Don't rewrite past entries; if a fact turns out wrong, add a correcting bullet on the day the correction was made.

---

## 2026-05-13

- Result: **All 3 × 2xA100 smokes + 2xA100 prod 5-step OOM check PASS on ALICE PCIe (node871)**. Overnight autonomous session 23:55 CEST 2026-05-12 → morning 2026-05-13 inside srun 2306117. Step 3 generation at prod shape (320 traj/step, seq=8192, micro=1) completed cleanly — the activation-memory failure point Vast micro=2 hit (OOM) is now conclusively safe at ALICE micro=1. Step times for prod (untrained-cold-rollouts phase): step 1 = 2580.96s = 43 min, step 2 = 2460.83s = 41 min, step 3 generation passed without OOM at 09:11 CEST; full results in [`milestone_5/HARDWARE_FIXES_2026-05-13.md`](milestone_5/HARDWARE_FIXES_2026-05-13.md).
- Bugs caught + fixed overnight (full table at [`milestone_5/HARDWARE_FIXES_2026-05-13.md §2`](milestone_5/HARDWARE_FIXES_2026-05-13.md)): (1) Ray dashboard subprocess silently dies on ALICE apptainer SIF → main `ray.init()` crashes; fix at `nemo_rl/distributed/virtual_cluster.py:170` with `include_dashboard=False` (commit `bc49e0d`); (2) `m5_verify_seq*.sh` inherited `SLURM_SUBMIT_DIR` from wrapper → inner sbatch precheck looked for retriever index in wrong dir → all 4 stages bailed in 1-5s; fix `unset SLURM_SUBMIT_DIR` (ALICE-side); (3) `sbatch_m5_1.sh`/`sbatch_m5_5.sh`/`sbatch_m5_6.sh` (the 1xA100 entry scripts) lacked `--` pass-through for Hydra overrides; fix added now (this commit); (4) **node870 is genuinely degraded** — Python cold-start takes 1h 43m on node870 (vs 10-20 min on node871/872), then Ray init still times out. All new sbatches use `--exclude=node870`.
- Decision: **Pre-warm pattern validated** — `dd if=<index> of=/dev/null bs=4M` + force-load encoder + force-load venv python before retriever launch cuts retriever cold-boot from **36 min → 65 s** (~30× speedup) when zfsstore page cache survives. Burns ~30 s of compute time. Pre-warm baked into the new v2 sbatches.
- Submitted: **12 new safety-net sbatches** (full table at [`milestone_5/HARDWARE_FIXES_2026-05-13.md §5`](milestone_5/HARDWARE_FIXES_2026-05-13.md)): (a) 2312311 cold-prod-shape 2xA100 (3 steps, NO pre-warm — tests whether queued prod will work fresh), starts 2026-05-13 11:20 CEST; (b) 2312312 cold-prod-shape 1xA100, starts 19:25 CEST; (c) 2312326 + 2312327 autonomous smoke verifier v2 (2xA100 + 1xA100, with pre-warm + `--exclude=node870`); (d) 2312328-2312333 six v2 prod sbatches (m5_1/5/6 × 1x/2x) with pre-warm + node870 exclusion + `HF_REPO_PREFIX=...-v2` to avoid HF Hub collision with originals. All `gpu-short` for (a-c), `gpu-a100-80g` 7-day for (d).
- Status: original 6 prod sbatches (2276314-2276319) submitted 2026-05-12 12:37 CEST remain PENDING with `StartTime=Unknown` (Priority queue, no firm date). SLURM cannot estimate — bottleneck is the 7-day walltime + May 18 maintenance reservation on node[863-865, 867, 871-876, 885]. Could be days before they start. The new gpu-short validators give us today's verification data well before originals fire.

## 2026-05-12
- PM: 2xA100 TP=2 verification on Vast surfaced a NeMo-RL FSDP+TP>1 bug (vocab-parallel embed/lm_head crash on `reset_sharded_param`); fixed via `custom_parallel_plan` that excludes embed_tokens/lm_head from sharding (commit `ca5386e`). Also caught + fixed a pre-existing duplicate `cluster.num_nodes` parse-bug in all 3 prod_2xa100 yamls (commit `590b4bd`). Option B fallback (`tensor_parallel_size: 1`) committed first as safety (`0041727`). micro_batch_size=2 OOMs at step 3 from fragmentation; staying at micro=1. Full writeup in [docs/milestone_5/HARDWARE_FIXES_2026-05-12.md §12](milestone_5/HARDWARE_FIXES_2026-05-12.md#12-vast-2xa100-sxm4-verification--option-a-upgrade-2026-05-12-pm).

- Result: **Vast 2xA100 verification session (evening) — Option A `custom_parallel_plan` unblocks TP=2 on Qwen3.5; micro=2 doesn't fit; ALICE cold-start surprise surfaces**. Rented Vast 23.127.144.217 (2× A100-SXM4-80GB NVLink) to verify the 2xa100 prod path before ALICE's prod sbatches fire May 14-19. Four findings, full trail in [`milestone_5/HARDWARE_FIXES_2026-05-12.md` §12-§14](milestone_5/HARDWARE_FIXES_2026-05-12.md): (a) initial 2xa100 prod 1-step crashed in FSDP `_reset_sharded_param` with length=248320 vs 124160 (Qwen3.5 vocab 248320 / 2 ranks) — `pytorch/pytorch#136228` bug class on vocab-parallel embed/lm_head. Root cause: NeMo-Automodel has no built-in parallel plan for `Qwen3_5ForConditionalGeneration`, heuristic auto-shards embed/lm_head. (b) Fix: new `training_m5_X/src/parallel_plan_qwen35.py` excluding embed/lm_head from sharding (replicated instead), wired into all 6 × 2xa100 yamls via `dtensor_cfg.custom_parallel_plan`. Verified by 3 smoke_2xa100 PASSES + 2xa100 prod 1-step PASS at micro=1. (c) Also caught + fixed pre-existing duplicate `cluster.num_nodes` in 3 prod 2xa100 yamls (silent YAML last-wins, would have crashed ALICE prod sbatches if a sibling change reordered keys). (d) Three attempts to enable `train_micro_batch_size=2`: vmem=0.7 + default allocator → OOM step 3; vmem=0.55 → still OOM step 3; vmem=0.7 + `PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:64` → past failure point but **30× slowdown** (step 1 training 32+ min vs ~2 min historically). Killed. Verdict: ship `micro=1` to ALICE. Real unblock for micro=2 is patching `nemo_rl/distributed/model_utils.py:1378` to chunk the logits→fp32 cast (deferred to M5.3, same scar as M5.2 v7 postmortem). Commits `0041727 590b4bd ca5386e 4aa7168` all pushed to `origin/experiment_1_alice`; ALICE working dir pulled cleanly (fast-forward from 4 commits behind).
- Result: **ALICE 2274508 surfaced previously-unmeasured cold-start I/O cost — retriever boot 36 min, training Python stuck `Dl` 1h+**. First ALICE training run today to actually launch end-to-end (prior 2266786 m5_smoke_alloc + 2266839 m5_smoke_mig srun allocations were cancelled at 10:46 CEST before reaching the training step). Retriever cold-boot measured at 2157s = 36 min (8 workers × E5-base-v2 199-shard load + 16 GB IVF-SQ8 index mmap, cold-read from zfsstore at ~40 MB/s; consistent with 5400s timeout). After retriever healthy, `run_grpo.py` (PID 1134955) launched at 22:36 CEST; as of 23:43 CEST showed wall 1h 07 min, **CPU 23 seconds total** (<0.5% CPU), state `Dl`, GPU idle, retriever served 0 retrieval requests. Most likely cause: apptainer squashfuse_ll serialising the SIF rootfs cold-read for Python stdlib + torch + nemo_rl + vllm imports. User constraint: `RETRIEVER_NUM_WORKERS=8` is the rollout-throughput floor; can't dial back. Mitigation plan ([`milestone_5/HARDWARE_FIXES_2026-05-12.md` §13.4](milestone_5/HARDWARE_FIXES_2026-05-12.md)): (1) warm-cache test at 2x verify 02:51 CEST + srun 06:55 CEST — does prior zfsstore-cache from 2274508 cut boot to 5-10 min on same node? (2) pre-warm at sbatch start via `vmtouch -t` + `find | cat > /dev/null` on index + venv + hf_cache; submitted as sbatch **2293032** (`m5_1_smoke_check_sbatch.sh`) at 00:13 CEST, queued behind 7 priority-blocked jobs, SLURM est. start 10:55 CEST 2026-05-13. (3) Stage to node-local scratch ($TMPDIR) — deferred. Saved as memory `project_retriever_cold_boot_alice.md`.
- Created: [`milestone_5/M5_2X_VERIFY_RUNBOOK_2026-05-13.md`](milestone_5/M5_2X_VERIFY_RUNBOOK_2026-05-13.md) — operator runbook for tomorrow's 06:55 CEST 2xA100 srun. 6 sections: §0 pass criteria (no FSDP `_reset_sharded_param` crash, no OOM, step time 8-18 min, ckpts save, retriever ≤15 min on warm cache), §1 pre-srun read of 2275683 autonomous output, §2 inside srun (attach + 3 × 2xa100 smoke with explicit pass/fail branches per smoke), §3 prod-shape 10-step OOM test at micro=1 (m5_1 with `grpo.max_num_steps=10` Hydra override; ~60-100 min), §4 decision matrix (6 outcome rows mapping smoke/prod results to which prod sbatches to scancel), §5 backup window using 1xA100 srun 2293289 at 12:55 CEST. Plus salloc'd interactive 1xA100 reservation **2293289** (4h, starts 2026-05-13 12:55 CEST) so prod sbatches have a debug window between 2xA100 verify and their May 14 firm-start.
- Decision: **Tomorrow's verification job is to validate sbatches inside the 06:55 CEST 2xA100 srun (2275986) per the new runbook**. The 02:51 CEST sbatch 2275683 runs `m5_verify_seq_2xa100.sh` autonomously (3 smoke_2xa100 + stage 4 prod_2xa100 1-step OOM check); the 06:55 srun is the interactive follow-up: (a) read 2275683 output first, (b) re-run 3 × 2xa100 smoke (m5_1 + m5_5 + m5_6) inside the srun while watching GPU mem live, (c) run m5_1 prod_2xa100 10-step OOM test at micro=1 to validate steady-state activation memory + step time, (d) apply the §4 decision matrix to scancel any prod 2xa100 sbatch (2276315/2276317/2276319) whose verification fails before they fire May 14-19. Total srun budget: ~2-2.5h of 4h walltime, comfortable.
- Decision: **`experiment_1_alice` branch — M5.1/M5.5/M5.6 sbatches hardened for ALICE; 12 commits, 11 bugs fixed before resubmit**. Single working session on ALICE (Apptainer SIF + zfsstore + SLURM, vs Vast.ai Docker + NVMe where the sbatches originated). Full inventory + math in [`milestone_5/HARDWARE_FIXES_2026-05-12.md`](milestone_5/HARDWARE_FIXES_2026-05-12.md). The 11 bugs: (1) venv pre-check followed symlink into container → `-d` on dir; (2) M5.5/M5.6 2x sbatch config-filename typos; (3) ckpt config crash (already fixed in research_v2 a1 postmortem, ported to all 6 prod yamls); (4) HF watcher repo-name collision across experiments → per-experiment `-m5_X_` prefix; (5) smoke yamls didn't exercise ckpt save → enabled + save_period=2 + max_num_steps=4; (6) retriever `/health` timeout 1800s too low for cold zfsstore (8 retrievers × ~7 min cold-read each) → bumped to 5400s; (7) HF watcher apptainer-exec didn't forward `CHECKPOINT_DIR_BASE` → watcher polled the wrong path; (8) `--mem=160g/200g` too small — user caught my "17 GB total" misreading; 8 retrievers × `faiss.read_index()` = 8 × ~17.5 GB = ~140 GB + 60 GB trainer + 15 GB overhead = ~215 GB → bumped to 240g (node has 252 GB); (9) Ray's default `/scratchdata` is RO in apptainer rootfs → bind-mount per-job writable dir; (10) DTensor `tensor_parallel_size: 1` in 2xa100 yamls (DDP across 2 GPUs, not TP=2) → flipped to 2 in all 3 2xa100 prod yamls per HARDWARE_COMPARISON §3 analysis; (11) DTensorPolicyWorkerV2 venv directory existed but empty → downloaded + extracted 12 GB tarball from `pantomiman/reason-over-search-v1-venvs`. Plus added `--` pass-through to sbatch_m5_1_2xa100 for Hydra overrides (stage-4 prod 1-step OOM check). Open issue (not blocking resubmit): HF watcher container-lifetime — squashfuse_ll times out between polls so only ~1 ckpt uploads per training run; tracked as follow-up.
- Decision: **Plan B (Option B) verification in-srun on gpu-short — m5_1 PASSED rc=0 end-to-end** (ckpt step_2 + step_4 saved at 6.36 GB each; WANDB run `5n2gy5r7` in project `reason_over_search_m5_1`; HF Hub upload step_2 confirmed at `pantomiman/qwen3.5-0.8b-grpo-musique-m5_1_smoke-seed42-step2`; HF step_4 missed only because the container's squashfuse_ll timed out before the second poll cycle). m5_5 + m5_6 in progress at log time. Plan B used `RETRIEVER_NUM_WORKERS=4` (fits 120 GB srun); the 8-retriever shape is math-verified (8 × 17.5 = 140 GB fits 240g) and will be in-allocation verified tomorrow by sbatch 2274508 (gpu-short, 240g, 8 retrievers, StartTime 2026-05-13 08:23 CEST).
- Decision: **2xa100 verification scheduled for tomorrow on gpu-short**. sbatch **2275683** (auto-fires `m5_verify_seq_2xa100.sh` — 3 smoke_2xa100 + stage 4 m5_1 prod_2xa100 1-step OOM check at full prod batch shape) lands 2026-05-13 13:50 CEST; srun **2275986** (interactive 2× A100, 4h walltime) lands 2026-05-13 17:50 CEST. Both before the prod 2xa100 sbatches' firm StartTimes (M5.1 2x May 15, M5.6 2x May 18, M5.5 2x May 19) → 1-3 day buffer to scancel + retune if 2xa100 verification surfaces issues. New [`training_m5_X/configs/m5_smoke_2xa100.yaml`](../training_m5_1/configs/m5_smoke_2xa100.yaml) (Option C-1; mirrors `m5_smoke.yaml` with TP=2 + gpus_per_node=2 + vllm gpu_memory_utilization=0.7) + new `smoke_2xa100` mode-case in each `run.sh`.
- Result: **2x A100 prod 1-srun fit projection — comfortable with `micro=2`, marginal at `micro=1`**. From a1's 23.7 min/step anchor (steps 38-42, "improve-and-grow" phase), HARDWARE_COMPARISON.md §3 projects: 2× A100 TP=2 at micro=2 → ~8.8 min/step → ~91 h (3.8 d), fits 7-day walltime with 77 h buffer; 2× A100 TP=2 at micro=1 (current 2xa100 prod yaml, conservative pending the 2-GPU smoke) → ~15.8 min/step → ~164 h (6.8 d), fits with only ~4 h buffer. Tomorrow's stage-4 OOM check at micro=1 confirms whether the conservative path works in practice; flipping to micro=2 later (after a dedicated 2-GPU smoke) gives the comfortable budget. Full sensitivity table in [`milestone_5/HARDWARE_FIXES_2026-05-12.md` §7](milestone_5/HARDWARE_FIXES_2026-05-12.md#7-sensitivity-to-a3-step-time-rollout-length-drift).

## 2026-05-11

- Decision: **M6 Phase-1 salvage + data audit authored** (this commit). Two new docs in [`milestone_6/`](milestone_6/): [`PHASE_1_SALVAGE.md`](milestone_6/PHASE_1_SALVAGE.md) (three Phase-1 findings reusable as paper motivation, with file:line cites and limit-of-claim per finding), [`DATA_AUDIT_PHASE_1.md`](milestone_6/DATA_AUDIT_PHASE_1.md) (deep-dive on what is measured cleanly vs what is missing entirely + per-finding gap-closing experiment with cost). Authored ahead of the planned M6 phase order because the M5.1 wall-clock (~10-13 d) means experiment selection cannot wait for the landscape pass. Audit elevates Candidate C (reward-shape ablation: F1+0.1-floor vs F1-only vs EM-only at the M5.1 recipe) from provisional candidate to likely pick #1 — the only candidate that closes an unfilled-by-the-literature gap, is supported by a Phase-1 motivation paragraph the project already owns, and directly addresses ReSearch's own "biggest gap to exploit" admission. Three salvage findings: (1) 0.1 partial-credit floor masks tool-use signal (3-6 pp tool/no-tool gap vs 9 pp prompt-driven swing); (2) no-example + decision-rules prompt pareto-dominates with-example (held-out EM +9 %, ACC +12 %, F1 +14 % at half budget); (3) "shrink-and-improve" RL regime (3× length/call compression while reward grows, inverts long-CoT regime). Five cross-cutting gaps documented: no multi-seed runs, no per-rollout analysis, no 0.8B prompt-sweep replication, no direct format-OK-but-F1=0 rate measurement, no contrastive long-CoT baseline.
- Decision: **Milestone 6 created** as a planning-only milestone parallel to M5.1 wall-clock. Objective: pick the next two experiments + publication framing before M5.1 lands (M5.1 ETA ~10-13 d; experimentation must close 2026-06-10). Three new docs under [`milestone_6/`](milestone_6/): [`MILESTONE_6.md`](milestone_6/MILESTONE_6.md), [`CONVERSATION_CONTEXT.md`](milestone_6/CONVERSATION_CONTEXT.md), [`LOG.md`](milestone_6/LOG.md). Three frozen artifacts planned: `LANDSCAPE_TABLE_2026-05.md` (Phase 1, 2026 competitive landscape table covering Tree-GRPO ICLR 2026, JustRL ICLR 2026 blogpost, MC-GRPO, DIVA-GRPO, Agentic-RL survey 2509.02547), `CANDIDATE_EXPERIMENTS.md` (Phase 2, 3-5 candidate pairs with cost / criterion / risk), `PICKED_PAIR.md` + `PUBLICATION_FRAMING.md` (Phase 3). Triggered by 2026-05-11 critical review (this conversation) which found: (a) "Search-R1 + ReSearch combined" framing is inaccurate — M5 is ReSearch alone; (b) MC-GRPO on search-tool is a genuine gap but Tree-GRPO ICLR 2026 already targets the efficient-rollouts axis; (c) NeurIPS main acceptance probability with current plan is ~5-10 %, realistic targets are Findings / Workshop / ICLR blogpost.
- Decision: **M5.1 paper-faithful production config authored + Phase-1 projection** (commit `037c6a5`). [`training_m5_1/configs/m5_1_research_paper.yaml`](../training_m5_1/configs/m5_1_research_paper.yaml) lands at the production shape: `num_prompts_per_step=64`, `G=5` (320 traj/step), `max_total_sequence_length=8192`, `max_new_tokens=1024`, `max_rollout_turns=10`, 622 steps × 2 epochs (199,380 trajectories total MuSiQue train). Group-C paper-faithful flips applied (LOO off, warmup 0, max_obs_chars 1024). Pre-measurement scaling estimate from smoke v6 (93.1 s/step at micro=2 + 20 traj/step) projected ~29 min/step × 622 = **12.5 d** at \$1.50/h ≈ \$450 — within the [`todo/TODO_2026-05-04.md`](todo/TODO_2026-05-04.md) ≤10 h ablation bar's neighbourhood. Projection later revised: see v7 OOM bullet below.
- Result: **M5.2 v7 baseline OOM postmortem → micro=1 forced + 25 d projection** (commits `d28b36a`, `fb4147d`). v7 attempt 1 at the production shape (seq=8192, `train_micro_batch_size=2`) crashed at step 2 with `log_softmax` allocating 13.73 GiB on top of 13.11 GiB free — off by 0.62 GiB. Root cause: NeMo-RL v0.6.0's TP=1 path at [`distributed/model_utils.py:1378`](../training_m5_1/nemo_rl) casts the full `[B,S,V]=[2,8192,248320]` logits to fp32 before any chunking; `logprob_chunk_size` only wires to `LogprobsPostProcessor`, not `LossPostProcessor`. Two unblocks: (a) patch model_utils.py:1378 to chunk the cast (unblocks micro=2 → halves training wall; **deferred to M5.3** — out of overnight budget), (b) drop to `train_micro_batch_size=1` (taken). v7 attempt 2 at micro=1 measured 55.6 → 59.1 min/step; ~57-60 s/step steady-state → 622 × ~58 min = **~25 d at \$1.50/h ≈ \$900**. M5.2 system-gain iteration plan (v8-v12, one lever per smoke) was **truncated to a bundled O1 + R2 commit** (O1 fused AdamW, R2 vLLM async_engine — both textbook-safe shipped features; ~1-3% combined; doesn't require smoke validation); R4 (gpu_mem_util 0.5 → 0.6) skipped because the OOM was already 0.62 GiB into the red. Postmortem detail + decision log in [`report/RESULTS_SMOKE_m5.md` §3, §5, §7](report/RESULTS_SMOKE_m5.md).
- Result: **M5.1 production training launched** (~01:05 UTC, pid 178440, exp_010, W&B run `uwbodqgt`). Boot-time fix landed first ([`db0852b`](report/RESULTS_SMOKE_m5.md)): val disabled + ckpt metric switched to `train/loss/mean` + `keep_top_k=0` (the `val_at_end: true` + `data.validation: null` crash from M5.1-a1 is an at-boot assertion in NeMo-RL; MuSiQue dev split not prepared by [`scripts/prep_musique.py`](../training_m5_1/scripts/prep_musique.py) anyway, eval is out-of-band via [`evaluation_qwen35/`](../evaluation_qwen35/)). First checkpoint lands at step 50 (`checkpointing.checkpoint_dir=results/grpo/m5_prod/seed42/`, save every 50 steps). Live per-step trajectory in [`report/RESULTS_SMOKE_m5.md` §6.2](report/RESULTS_SMOKE_m5.md).
- Result: **Live trajectory through step 10 — ETA tightening from ~25 d → ~10-13 d as gen length collapses**. Per-step wall dropped 57.9 → 21.3 min over 10 steps (gen length 7038 → ~950 tokens; truncation rate 68.4 % → 15.9 % at step 8 per W&B; reward mean rising 0.020 → 0.123 peak at step 9). Phase shares stable (training ~70%, logprobs ~20%, generation ~8%); absolute wall drops proportionally from each phase as rollouts shorten. Step-10 timing breakdown added to [`report/RESULTS_SMOKE_m5.md` §6.5](report/RESULTS_SMOKE_m5.md); revised ETA table in §6.4 (~21–29 min/step → ~10–13 d). The trend is still pointing down at step 10 — if it stabilises near ~20 min/step, total wall drops to ~9–10 d.
- Decision: **HARDWARE doc split — `HARDWARE_4090.md` (historical 4090 dev-box) + `HARDWARE_COMPARISON.md` (current accelerator + provider comparison)** (commit `540d743`). Triggered by the 1× A100 ETA and the question of whether to swap hardware to land a single M5.1 run in 1-2 d. Old [`setup/HARDWARE.md`](setup/HARDWARE_4090.md) renamed to `HARDWARE_4090.md` (preserves the 4090 dev-box snapshot used through M0-M1, plus the generic accelerator-spec table for historical reference). New [`setup/HARDWARE_COMPARISON.md`](setup/HARDWARE_COMPARISON.md) anchored on the live step-8 per-step breakdown (33 min/step on A100, 71.6% training / 20.5% logprobs / 6.9% generation), with per-config wall-clock + cost estimates: **1× B200 ≈ 1.5–2 d ≈ \$250** on RunPod (recommended primary), 1× H200 ≈ 3.5–4 d (clean fallback), 1× H100 ≈ 7 d, 2× H100/H200 with TP=2 ≈ 1.5–3 d. Vast vs RunPod May-2026 pricing snapshot included; Blackwell sm_100 + v2 worker venv compat caveat flagged as the open question before B200 commit (resolvable via 30-min smoke). 6 cross-doc references repointed: generic "accelerator comparison" pointers (SETUP_VAST, BOOTSTRAP_NEW_INSTANCE, VAST_AI_PLAN_A, TODO_2026-05-04, EVAL_OPS) → `HARDWARE_COMPARISON.md`; M1-era 4090 dev-box pointers ([`report/CODE_SETUP_m1.md`](report/CODE_SETUP_m1.md)) → `HARDWARE_4090.md`. Archive-doc references left as historical pointers.
- Lint: `python3 scripts/wiki_lint.py docs/` first reported 2 broken `.md` links I introduced via the HARDWARE rename — [`archive/BOOTSTRAP_NEW_INSTANCE_v0.md`](archive/BOOTSTRAP_NEW_INSTANCE_v0.md) and [`archive/VAST_INSTANCE_SETUP_v0.md`](archive/VAST_INSTANCE_SETUP_v0.md) both pointed at the old `setup/HARDWARE.md`. Fixed in this commit by repointing to [`setup/HARDWARE_4090.md`](setup/HARDWARE_4090.md) (the historical-content target the originals meant). Re-run is **OK (warnings only)**: remaining 5 warnings are pre-existing intentional broken links (`RESULTS_QWEN3_BASELINE.md` Jose-owned forward-ref, `internship/*` gitignored, `MILESTONE_5.md` `PAPER_VS_OURS_M5.md` forward-ref already in INTENTIONAL_BROKEN), 1 orphan ([`milestone_5/MILESTONE_5_3.md`](milestone_5/MILESTONE_5_3.md), forward-ref placeholder), 1 missing-frontmatter ([`milestone_4/M4_PROMPTS_SCRATCH.md`](milestone_4/M4_PROMPTS_SCRATCH.md), scratch doc). Separately, a custom out-of-scope check flagged 2 broken `.py` references in [`report/CODE_SETUP_m1.md`](report/CODE_SETUP_m1.md) (`evaluation_search_r1/flashrag/search_r1/{templates,answer_utils}.py` — directory renamed to `evaluation_qwen35/` at some prior M4 point); `scripts/wiki_lint.py` LINK_RE only checks `.md` so these are outside the project lint contract, deferred to a follow-up housekeeping commit. Markdown sanity (code-fence balance, frontmatter, table column consistency) clean on new/changed files.
- Correction: **HARDWARE_COMPARISON pricing-page verification — Lambda B200 is actually $6.99/h not $3.49/h; Spheron $2.25/h is the cheapest verified single-GPU**. User flagged that my Lambda prices didn't match their console (live screenshots: Lambda HGX B200 cluster shows $8.87-9.86/h, 16 GPU min, 2 week min; Spheron pricing page shows B200 single-GPU at $2.25/h). Fetched each provider's actual pricing page directly via WebFetch and found **multiple errors** in prior numbers from web-search snippets — Lambda B200 single-GPU is **$6.69-6.99/h** (~2× my prior claim from search summary), Lambda H100 SXM is **$3.99-4.29/h** (not $2.49), Lambda A100 single-GPU is **40 GB only** (doesn't fit M5.1 at all), CoreWeave **doesn't offer single-GPU B200 at all** (only 8-GPU HGX pods at $68.80/h on-demand, $34.11/h spot — can't rent fewer than 8). Spheron has the actually-cheapest prices across the board: B200 $2.25/h, H100 $1.33/h, H200 $1.56/h, A100 80GB $0.72/h (cheaper than Vast's $1.20). Also flagged Lambda GH200 at $2.29/h (96 GB) as a **trap** — Grace Hopper is ARM, our x86 venv stack would need a complete rebuild (weeks of engineering for ARM64 wheels of torch/vllm/mamba-ssm/flash-attn/nv-grouped-gemm). Same logic kills GB200 NVL72. Updated [`setup/HARDWARE_COMPARISON.md`](setup/HARDWARE_COMPARISON.md) §4 to mark each row ✓ verified / ⓘ live-screenshot / ⚠ third-party-not-directly-verified, added verification-methodology note at top, added explicit "Why we're NOT recommending GH200 / Lambda 40GB-A100 / RTX PRO 6000" subsections. §5 cost-per-run table re-sorted with corrected prices; §6 recommendation reordered: (A) Spheron B200 ~$96 (cheapest, T3 reliability caveat), (B) RunPod Community $5.49 ~$206 (one-click T2), (C) Lambda B200 ~$257 (T1 premium). Vast m:71029 at $8.209/h now dominated by 4 cheaper B200 options instead of 6 (after correcting Lambda price). Also surfaced Spheron A100 80GB $0.72/h as an A100 fallback (~$167 for remaining wall vs ~$278 on Vast). wiki_lint: OK (warnings only). **Lesson learned**: never cite GPU prices from web-search snippets — fetch the actual pricing page.
- Decision: **HARDWARE_COMPARISON exhaustive provider scan — Lambda Labs B200 at $3.49/h is the new Pareto pick (LATER CORRECTED — see above)**. After two rented-listing screenshots came in (Vast m:71029 B200 at $8.209/h; RunPod B200 at $5.49/h + H200 SXM $3.99/h + H100 SXM $2.99/h + RTX PRO 6000 96GB at $1.89/h Unavailable), did a parallel WebSearch + WebFetch scan across 20+ verified providers ([getdeploying.com](https://getdeploying.com/gpus/nvidia-b200) 22+ B200, 30+ H200, 42+ H100): Spheron B200 spot $2.12/h is the absolute floor (T3 marketplace, preempt risk); Lambda Labs B200 on-demand $3.49/h is the **best risk-adjusted** (T1-reliability, dropped from $3.79 in April 2026); CoreWeave $5.50/h on-demand; Jarvislabs cheapest H200 at $3.80/h; RunPod B200 spot $3.59/h (recent drop from $5.98). The Vast m:71029 listing at $8.209/h is dominated by 6 cheaper B200 alternatives. Updated [`setup/HARDWARE_COMPARISON.md`](setup/HARDWARE_COMPARISON.md) §4 with the full provider-by-GPU table (B200, H200, H100 each), §5 cost-per-run table re-sorted cheapest→most expensive, §6 recommendation flipped to "Lambda B200 production / Spheron spot cost-min / Lambda H100 SXM Blackwell-fail fallback". Sanju's TODO swap sequence (wait for step 50 → smoke B200 on Lambda → flip micro=2 → launch) costs ~$138 total. Also explicitly downgraded RTX PRO 6000 ($1.89/h, 96GB) from "watch list" to **skip**: GDDR7 mem-BW (~1.79 TB/s) is *lower than A100* (2.0 TB/s) → memory-bound phases (32% of step) would actually be slower than A100; workstation thermals not built for 80+ h sustained 100% util; sm_120 driver path uncharted. The cheap-on-paper $1.89/h trap masks the real cost: ~$190-220 / ~4-5 d with stability risk. wiki_lint: OK (warnings only).
- Result + Decision: **HARDWARE_COMPARISON v1 → v2 — per-step trend reversed (U-shape minimum at step 17, climbed to 24 min/step by step 42)**. Pulled live-log data through step 42: per-step bottomed at step 17 (10.4 min) and then climbed monotonically to 23.7 min/step (steps 38-42 mean) as the model entered an "improve-and-grow" regime — rollout length re-expanded 636 → ~1000 tokens, reward climbed 0.10 → 0.19. The "shrink-and-improve" regime (steps 1-17) and "improve-and-grow" regime (step 18+) are distinct learning phases; both are healthy, but only the first drove the v1 anchor (10.6 min/step trough) which is now invalid. v1 archived to [`archive/HARDWARE_COMPARISON_v1.md`](archive/HARDWARE_COMPARISON_v1.md) with deprecation banner; new [`setup/HARDWARE_COMPARISON.md`](setup/HARDWARE_COMPARISON.md) (v2) recalculates from the 23.7 min/step anchor — every config's wall + cost ~2.3× higher: A100 full run **10.2 d / ~\$294** (was 4.5 d / \$130); 2× H100 SXM TP=2 1.7 d / ~\$172 (was 22 h / \$82); 1× B200 ~34 h / ~\$220 (was 14-16 h / \$90). Recommendation flipped: **port now** is the new Pareto pick (B200 saves ~9 d AND ~\$74 vs letting A100 finish; 2× H100 TP=2 saves ~\$122 + 8.5 d). v2 adds a §0 cost-shift table making the v1→v2 delta explicit. Also updated [`report/RESULTS_SMOKE_m5.md` §6.4](report/RESULTS_SMOKE_m5.md#64-eta--revised) ETA table with the PM row + U-shape explanation; updated [`todo/TODO_2026-05-11.md`](todo/TODO_2026-05-11.md) "Active target" with the revised cost table + recommended swap sequence (wait for step-50 ckpt → smoke target hw → launch swap → kill A100 once swap healthy). Sunk cost on A100 to step 42: \$19 / 15.7 h. wiki_lint: OK (warnings only).
- Decision: **Portability review pass + TODO_2026-05-11 authored for Sanju + `experiment_1` branch cut**. Triggered by the M5.1-running state and the question of whether to port to B200 on RunPod next. Wholistic review (audit via Explore subagent) surfaced 4 categories of doc staleness blocking a clean port; all fixed in commit `2227291`: (1) [`claude/CLAUDE.md`](../claude/CLAUDE.md) — stale `docs/HARDWARE.md` repo-layout line replaced with HARDWARE_COMPARISON.md + HARDWARE_4090.md + v0 archive pointers; (2) [`docs/setup/SETUP_INSTANCE.md`](setup/SETUP_INSTANCE.md) (then `docs/vast/SETUP_VAST.md`) "Time + cost expectations" — M2 row (1005 steps, 11-17 d) replaced with live M5.1 table (4.5 d on A100, ~2 d on H100 SXM, ~14-16 h on B200; per-config table cross-ref to HARDWARE_COMPARISON), added "Porting to non-Vast hosts (RunPod)" section flagging `/workspace` assumption + Blackwell smoke prereq; (3) [`training_m5_1/README.md`](../training_m5_1/README.md) — `m5_1_research_paper.yaml` marked LIVE (was TBD), launch block updated with live per-step anchor; (4) [`docs/report/CODE_SETUP_m5.md`](report/CODE_SETUP_m5.md) — §3.3 smoke-vs-prod knob table fully populated from live yaml (was 11 rows of TODO; now 26 rows covering grpo/loss/policy/optimizer/generation/data/env/checkpointing), §4 M5.1 Critical Changes promoted from TODO spine to populated content (paper-vs-ours hyperparameter table, divergences explicit, reward implementation locked, revised wall-clock cross-ref). Deferred (not blockers): bootstrap.sh pre-flight checks for git-lfs + EVAL_PY fallback. Also authored [`todo/TODO_2026-05-11.md`](todo/TODO_2026-05-11.md) as the new catch-up entry (supersedes TODO_2026-05-10 as `CLAUDE.md` pointer), with the next-step decision (B200 port vs stay-on-A100) for Sanju. Branch `experiment_1` cut from `research_v2` at the M5.1-live snapshot (frozen; will not move) for reproducibility; continuing work on `research_v2`. wiki_lint: OK (warnings only).
- Decision: **HARDWARE_COMPARISON v0 archived → v1 authored on the new step-15-17 anchor (~10 min/step)**. Live trajectory continued accelerating after the previous bullet: step 17 landed at 10.4 min/step (vs step-8 anchor of 33 min); steady-state ~10–11 min/step, integrated full run ~4.5 d / ~\$130 on A100 (vs v0's 14-16 d / \$400-460). v0 archived to [`archive/HARDWARE_COMPARISON_v0.md`](archive/HARDWARE_COMPARISON_v0.md) with deprecation banner; new [`setup/HARDWARE_COMPARISON.md`](setup/HARDWARE_COMPARISON.md) recalculates per-config wall-clock + cost (every config now lands $75-150 / 0.6-2 d; cost-spread that justified "rent B200" has compressed away). New recommendation: **1× H100 SXM on Vast (~2 d, ~\$90)** is the Pareto point — halves wall vs status quo, cleanest swap, no Blackwell risk; B200 still fastest single-GPU (~15 h, ~\$90) if same-day finish is needed. Also added [Thundercompute](https://www.thundercompute.com/pricing) as a 4th provider (legit Cambridge-MA startup founded 2024, A100 80GB at \$0.78/h is the cheapest seen — but PCIe-only, no SXM/H200/B200, virtualized-GPU stack, beta, smoke required); useful as cheapest A100 swap if the live Vast A100 box ever flakes. Reliability assessment + pricing per [getdeploying.com review](https://getdeploying.com/thunder-compute), [Thundercompute A100 pricing](https://www.thundercompute.com/blog/nvidia-a100-pricing), [Thundercompute H100 pricing](https://www.thundercompute.com/blog/nvidia-h100-pricing). Live-trajectory anchor refresh in [`report/RESULTS_SMOKE_m5.md` §6.2-§6.5](report/RESULTS_SMOKE_m5.md) (steps 11-17 added).

---

## 2026-05-10

- Decision: **M4.4 extended plan landed** — extends the prompt search and full benchmark to BOTH variants (hybrid + base). Four phases queued, all deferred / next milestone TBD: **Phase 2** hybrid winner ablations (~30 min n=300; `terse+decide`, `terse+source_only`, `terse+research_role`; bar +0.04 over A); **Phase 3** hybrid full benchmark (~7 h n=51,713 × 7 datasets with the Phase-2 locked prompt; replaces [`report/RESULTS_m4.md`](report/RESULTS_m4.md) §4 hybrid row); **Phase 4** base variant prompt search (~30–45 min n=300; re-opens the M4.3 lock `qwen35_minimal_no_system` mean EM 0.010; 4 designed candidates incl. `qwen35_terse_no_system` and a re-test of auto-inject on base with a clean user message; bar +0.025 over M4.3 lock = 0.035 mean EM); **Phase 5** base full benchmark (~7 h n=51,713 with Phase-4 locked prompt; replaces §4 base row). Total wall ~15 h; Phase 2 + Phase 4 are independent (different model swap on SGLang) and parallelisable across boxes. Plan + candidate templates + open design questions in [`milestone_4/M4_4_PHASE_1B_DESIGN.md`](milestone_4/M4_4_PHASE_1B_DESIGN.md) §11 (Phase 2) and §12 (Phase 4/5). Status updated in [`milestone_4/MILESTONE_4.md`](milestone_4/MILESTONE_4.md) §M4.4 header + "What's left" rows 4a–4d. M5 scaffold work remains unblocked (can use the terse-locked hybrid prompt now; re-align if Phase 2 / 4 produce different winners).
- Result: **M4.4 Phase 1b — 10-candidate in-distribution prompt ablation — WINNER `qwen35_terse` closes the M3 cross-family gap**. Hybrid only, sequential 10-mode × 7-dataset sweep at n=300/dataset (bamboogle full split = 125), greedy / `seed=1`, optimised stack; wall-clock **1 h 52 min**. All 10 candidates use `<tool_call>` / `<tool_response>` via `tools=[QWEN35_SEARCH_TOOL]` auto-inject (in-distribution for Qwen3.5), `enable_thinking=True`, plain `<answer>X</answer>`; single-knob ablations only. 2 of 10 PASSED the +0.025 bar: **`qwen35_terse` Δ +0.0436 (mean EM 0.1030, all 7 datasets positive, binomial sign test p=0.0078)** — CLEAR winner — and `qwen35_research_role` Δ +0.0257 (marginal, system-role role-prime). 8 candidates failed: `qwen35_decide` Δ +0.0201 (narrow miss), `qwen35_source_only` Δ +0.0172, `qwen35_self_check` Δ +0.0133, `qwen35_multi_search` Δ −0.0058, `qwen35_decompose` Δ −0.0109, `qwen35_search_first` Δ −0.0156, `qwen35_hamlet_1shot` Δ −0.0228 (M3 1-shot anchor doesn't transfer), `qwen35_p3_decide_xml` Δ −0.0443 (94.4 % empty-pred collapse — system-role prose conclusively rules out M3.1 winner cross-family). **Headline**: terse mean EM 0.103 ≈ M3 untrained Qwen3-0.6B benchmark 0.102 → the entire M4.4-motivating cross-family gap is **closed** by removing redundant clauses from the user message (auto-injected `# Tools` + `<IMPORTANT>` block already covers them). Winning template is 3 sentences, ~30 user tokens: "Use the `search` tool to look up facts as needed. When you have the answer, write it inside <answer> and </answer>. For example, <answer> Beijing </answer>. Question: {prompt}". Largest absolute lifts on triviaqa (+0.091, +75 % rel.) and popqa (+0.079, +111 % rel.) — the open-domain entity-recall datasets where M4.2's bypass-and-fabricate failure was costliest. Per-candidate table + mechanism + per-dataset detail in [`milestone_4/MILESTONE_4.md` §M4.4 "Phase 1b results"](milestone_4/MILESTONE_4.md). Design doc with web-research-derived candidates + verbatim Python constants in [`milestone_4/M4_4_PHASE_1B_DESIGN.md`](milestone_4/M4_4_PHASE_1B_DESIGN.md). 10 new templates added to [`evaluation_qwen35/flashrag/search_r1/templates.py`](../evaluation_qwen35/flashrag/search_r1/templates.py); 8 user-locus modes registered in `_QWEN35_USER_PROMPT_MODES` in [`evaluation_qwen35/flashrag/pipeline/active_pipeline.py`](../evaluation_qwen35/flashrag/pipeline/active_pipeline.py). Also flagged: the prior E run had a routing confound (`qwen35_minimal_nothink` missing from `_QWEN35_USER_PROMPT_MODES` → template landed in system role with literal `{prompt}` dangling); E Δ −0.0278 confounds (a) closed-think with (b) locus swap. Phase 2 (winner ablations terse+decide, terse+source_only, terse+research_role) and Phase 3 (full n=51,713 sweep with terse-locked prompt to replace [`report/RESULTS_m4.md`](report/RESULTS_m4.md) §4) both unblocked. M5 scaffold work remains unblocked. Result files preserved at `evaluation_qwen35/results/<dataset>/<dataset>_*_m4_qwen3.5_0.8b_<mode>_seed1_n300/` for all 10 × 7 = 70 cells.
- Result: **M4.4 candidate E `qwen35_minimal_nothink` ran and FAILED the acceptance bar — worst Phase-1 result so far**. Hybrid only, n=300 / dataset, greedy / `seed=1`, optimised stack; wall-clock **6 min 17 s** (~3× faster than recall_port because closed-think collapses output tokens). Mean EM **0.0316** vs A baseline **0.0594** — **Δ −0.0278**, **5.3 pp below the +0.025 bar**. 6 / 7 datasets regress; per-dataset Δ: bamboogle −0.032, nq −0.048, triviaqa −0.095, popqa −0.054, hotpotqa −0.044, musique −0.007, 2wikimultihopqa **+0.086** (artifact, not signal). Empty-pred rate across the sweep is **57.7 %** (38.3–71.0 % per dataset) vs ~5–15 % for A — closing the open `<think>` block via `enable_thinking=False` removes the reasoning channel the hybrid relies on for search-loop control, not just the in-think entity hallucinations [`report/RESULTS_SMOKE_m4.md` §6.5](report/RESULTS_SMOKE_m4.md) hypothesised. The 2wiki "lift" is concentrated in binary comparison questions ("Who was born first, X or Y?", "Which film…"); model bypasses search and emits a confident fabrication of one of the two options → ~50 % chance correctness on that subset. On open-domain entity recall the same bypass yields ~0 %. Wiring cost was 5 LoC total (registry entry + per-mode override in `scripts/run_m4.sh`); the [HANDOFF](todo/HANDOFF_M4.4_2026-05-10.md)'s "Pipeline plumbing for `enable_thinking` per-mode" note was stale — `--enable_thinking` was already plumbed end-to-end. Render verified pre-launch (A=430 tokens, E=432 tokens; only the closing `</think>` differs). Per-dataset table + mechanism in [`milestone_4/MILESTONE_4.md` §M4.4 "Phase 1 results — qwen35_minimal_nothink"](milestone_4/MILESTONE_4.md). Result files preserved at `evaluation_qwen35/results/<dataset>/<dataset>_*_m4_qwen3.5_0.8b_qwen35_minimal_nothink_seed1_n300/`. Operational note: eval venv self-healed from transformers 4.57.1 → 5.7.0 (Qwen3.5 `model_type=qwen3_5` AutoConfig support) per [`training/scripts/bootstrap.sh`](../training/scripts/bootstrap.sh) line 73; first launch attempt errored cleanly with the documented misleading "architecture not recognized" message. Implication for Phase 1: **D (`qwen35_minimal_decide`, A + M3.1 decision sentences in user role) is now the highest-priority remaining test** — it's the one knob both recall_port and E left unperturbed. Open design question: refactor B / C to swap `<search>` tags for `<tool_call>` (in-distribution for Qwen3.5) before launch? Qwen3.5 was post-trained on `<tool_call>` / `<tool_response>`; both verbatim ports (B Search-R1, C M3.1) currently use `<search>` action tokens. M5 scaffold work remains unblocked.
- Result: **M4.4 first candidate `qwen35_recall_port` ran and FAILED the acceptance bar**. Hybrid only, n=300 / dataset (bamboogle full split = 125), greedy / `seed=1`, optimised stack; wall-clock 17 min 19 s. Mean EM **0.0510** vs A (`qwen35_minimal`) baseline **0.0594** — Δ **−0.0084**, well below the +0.025 lift the bar requires. 5 / 7 datasets regress; largest single drop is 2wikimultihopqa (−0.024, multi-hop), confirming that piling additional system-role prose on top of the auto-injected `# Tools` + `<IMPORTANT>` block crowds the load-bearing scaffolding rather than augmenting it (mechanism per [`report/RESULTS_SMOKE_m4.md` §7.3](report/RESULTS_SMOKE_m4.md)). Per-dataset table + interpretation now live in [`milestone_4/MILESTONE_4.md` §M4.4 "Phase 1 results — qwen35_recall_port"](milestone_4/MILESTONE_4.md). Implication for the rest of Phase 1: D (`qwen35_minimal_decide`, A's user message + M3.1 decision sentences) and E (`qwen35_minimal_nothink`, A + `enable_thinking=False`) are higher priority than C (M3.1 system-role port) since both keep the M4.2 user-role + auto-inject base intact and modify only one knob each — a milder system-role intervention (recall_port) already failed. Result files preserved at `evaluation_qwen35/results/<dataset>/<dataset>_*_m4_qwen3.5_0.8b_qwen35_recall_port_seed1_n300/`. M5 scaffold work remains unblocked.
- Decision: **M4.4 plan landed** — additive prompt-search sub-phase reopens M4 prompt selection (NOT the M4.2 baseline + cross-family results, which stay locked in [`report/RESULTS_m4.md`](report/RESULTS_m4.md)). Motivation: M4.2 hybrid `qwen35_minimal` lands at mean EM 0.0594; the M3 untrained Qwen3-0.6B hybrid — a smaller, older-family model — reaches 0.102 on the same 7 benchmarks (avg Δ −0.042 EM, 1.7×). Gap implicates the prompt, not parameters: Qwen3.5 was post-trained on different tool-use shapes than Qwen3, and we have not yet tested the prompt shapes that the literature (Search-R1, ReSearch / ReCall) and our own M3 / M3.1 ablations identified as load-bearing. Plan in [`milestone_4/MILESTONE_4.md` §M4.4](milestone_4/MILESTONE_4.md): Phase 1 = 5 candidates × 7 datasets × n=300 (~2 h on the optimised stack), acceptance bar +0.025 mean EM over the M4.2 lock; Phase 2 = ablate the Phase-1 winner (~25 min); Phase 3 = full sweep + lock. If no Phase-1 candidate clears the bar, ship M4.2 unchanged and move to M5 (4th prompt iteration; diminishing returns). Base variant stays at `qwen35_minimal_no_system` per the M4.3 lock — base lacks the tool-use prior. Phase-1 candidates: A=`qwen35_minimal` control, B=`qwen35_searchr1_pure` (Search-R1 verbatim, `<search>`/`<information>`), C=`qwen35_p3_decide_no_ex` (M3.1 winner shape, system-role + decision rules), D=`qwen35_minimal_decide` (A + M3.1 decision sentences), E=`qwen35_minimal_nothink` (A + `enable_thinking=False`). Companion: scratch comment file [`milestone_4/M4_PROMPTS_SCRATCH.md`](milestone_4/M4_PROMPTS_SCRATCH.md) shows the M4.2 / M4.3 locked prompts (template constants + post-`apply_chat_template` rendered view) for cross-machine review. Drafted ReCall-port template constant [`QWEN35_RECALL_PORT_TEMPLATE`](../evaluation_qwen35/flashrag/search_r1/templates.py) (verbatim Agent-RL/ReCall `re_call_template_sys` adapted to the M4 lock: nested-XML tags via `tools=[]` auto-inject, `<answer>X</answer>` not `\boxed{}`, search-style example) — NOT in the 5-candidate Phase-1 plan; standing by as a Phase-2 backup if a system-role candidate (C) wins. Templates B / D / E + `qwen35_p3_decide_no_ex` and pipeline branches still need wiring in [`evaluation_qwen35/flashrag/search_r1/templates.py`](../evaluation_qwen35/flashrag/search_r1/templates.py) and [`evaluation_qwen35/flashrag/pipeline/active_pipeline.py`](../evaluation_qwen35/flashrag/pipeline/active_pipeline.py); orchestrator script `scripts/orchestrate_m4_4.sh` not yet authored. Logged as Task #4 in `MILESTONE_4.md` "What's left"; M5 scaffold work is NOT blocked.
- Decision: **M4 close-out done**. Hybrid 2wikimultihopqa + musique FULL re-runs completed (14:40 → 17:01 UTC; 2 h 22 min); populated [`report/RESULTS_m4.md`](report/RESULTS_m4.md) with the full hybrid + base × 7-dataset table + cross-family M3-vs-M4 comparison. Headlines: hybrid mean EM **0.060**, base mean EM **0.010** (51,713 items / variant; greedy decode). Untrained Qwen3.5-0.8B (M4) is uniformly below untrained Qwen3-0.6B (M3 pre-GRPO mean 0.102) — average Δ **−0.042 EM** (−41 % rel); largest gaps on multi-hop (2wiki −0.10, popqa −0.06, triviaqa −0.05). The M4 floor is the locked target M5 GRPO must beat. Operational note: 2wiki + musique re-run was triggered by the eval venv reverting to `transformers==4.57.1` between sessions (breaks `AutoConfig.from_pretrained` on `model_type=qwen3_5`); fixed via `:v2` Docker image + bootstrap.sh self-heal. Closed M4 close-out item in [`todo/TODO.md`](todo/TODO.md).
- Decision: moved TODO files into [`docs/todo/`](todo/) (folder reorg) and added [`todo/TODO_2026-05-10.md`](todo/TODO_2026-05-10.md) as the current dated catch-up snapshot. Three files now: [`todo/TODO.md`](todo/TODO.md) (evergreen pending list), [`todo/TODO_2026-05-10.md`](todo/TODO_2026-05-10.md) (current state: M3+M3.1 closed, M4 12/14 cells, M5 plan landed, Docker `:v2` published), [`todo/TODO_2026-05-04.md`](todo/TODO_2026-05-04.md) (frozen older handoff). Bulk-updated 22 incoming references across the repo (`claude/CLAUDE.md` "If you only read one thing" now points at the 2026-05-10 snapshot; `docs/<subdir>/*.md` `../TODO_2026-05-04.md` → `../todo/TODO_2026-05-04.md`; inside the moved files, `../X` paths bumped to `../../X` for repo-root and `X/` prefixed with `../X/` for known docs subdirs). Folder index at [`todo/README.md`](todo/README.md) explains which file does which job.
- Decision: published Docker image `pantomiman/reason-over-search-v1:v2` (= `:v1` + `transformers==5.7.0` for Qwen3.5 `model_type=qwen3_5` AutoConfig support). Surfaced when the running M4 eval on Vast box `36465611` failed with `KeyError: 'qwen3_5'` after the eval venv reverted to transformers 4.57.1 between runs. New thin Dockerfile.v2 at [`docker/reason-over-search-v1/Dockerfile.v2`](../docker/reason-over-search-v1/Dockerfile.v2) (FROM `:v1` + one pip install; ~500 MB layer, all base layers shared). The original [`Dockerfile`](../docker/reason-over-search-v1/Dockerfile) was left unchanged because a from-scratch rebuild now hits an upstream NeMo-RL conflict (`automodel`+`vllm` extras became mutually exclusive after 2026-05-02). [`training/scripts/bootstrap.sh`](../training/scripts/bootstrap.sh) now idempotently upgrades transformers in the eval venv so a `:v1` box self-heals after bootstrap. Reference updates across [`docs/setup/SETUP_INSTANCE.md`](setup/SETUP_INSTANCE.md) (then `docs/vast/SETUP_VAST.md`), [`docs/setup/BOOTSTRAP_NEW_INSTANCE.md`](setup/BOOTSTRAP_NEW_INSTANCE.md), [`docs/milestone_2/MILESTONE_2.md`](milestone_2/MILESTONE_2.md), [`docs/milestone_2/PHASE_2_RUNBOOK.md`](milestone_2/PHASE_2_RUNBOOK.md), [`claude/CLAUDE.md`](../claude/CLAUDE.md), [`docker/reason-over-search-v1/README.md`](../docker/reason-over-search-v1/README.md) bumped runtime tag to `:v2`; archived/historical run reports retain `:v1` to record what they actually ran.
- Lint: full wiki audit + gap-fill pass. Pre-state: `python3 scripts/wiki_lint.py docs/` reported **22 broken links** (FAIL); post-state: **0 broken links** (`OK (warnings only)`, 12 intentional warnings retained for forward-refs + gitignored `internship/`). Real fixes applied: (1) [`milestone_1/MILESTONE_1.md`](milestone_1/MILESTONE_1.md) lines 11/109/133 had bare `RESULTS_m1.md` / `CODE_SETUP_m1.md` filenames left over from the milestone-rename sweep; repathed to `../report/RESULTS_m1.md` and `../report/CODE_SETUP_m1.md` (line 11 also had a duplicate ref from the merge of RESULTS_PLAN_B + COMPARISON_PLAN_B_v1 into RESULTS_m1; collapsed to a single ref). (2) [`milestone_1/MILESTONE_1.1_QWEN_BASELINES.md`](milestone_1/MILESTONE_1.1_QWEN_BASELINES.md) lines 159/202 same fix. (3) [`archive/COMPARISON_PLAN_B_v0.md`](archive/COMPARISON_PLAN_B_v0.md) lines 11/13/86/109: bare `MILESTONE_1.md` repathed to `../milestone_1/MILESTONE_1.md`, `RESULTS_PLAN_B.md` repathed to `RESULTS_PLAN_B_v0.md` (same-dir), and `[docs/archive/](../archive/)` (self-reference) replaced with `[COMPARISON_PLAN_B_SUSPECTS.md](COMPARISON_PLAN_B_SUSPECTS.md)`. (4) [`archive/COMPARISON_PLAN_B_SUSPECTS.md`](archive/COMPARISON_PLAN_B_SUSPECTS.md) line 11: stale `../milestone_one/` paths repathed to `COMPARISON_PLAN_B_v0.md` (same-dir) and `../milestone_1/MILESTONE_1.md`. Allowlist update: added `PAPER_VS_OURS_M5.md` to [`scripts/wiki_lint.py`](../scripts/wiki_lint.py) `INTENTIONAL_BROKEN` (forward-ref called out as TODO in the 2026-05-09 M5 launch entry; blocks M5.1 config, intentional). Root cause: the 2026-05-09 cross-doc link sweep that claimed "~216 references across ~50 files" missed the 4 files above; surfaced by an audit pass on whether wiki workflow rules (frontmatter, `log.md` updates, lint cleanliness) were actually being honoured per [`SCHEMA.md`](SCHEMA.md).
- Created: [`todo/TODO.md`](todo/TODO.md), evergreen pending-work tracker (sibling to the frozen `todo/TODO_2026-05-04.md` catch-up doc); folder reorg + 2026-05-10 catch-up snapshot landed in a follow-up commit. Sections: Infra (Docker `:v1` rebuild known-broken; `:v2` workaround), M4 close-out (hybrid 2wiki + musique re-run), M5 (PAPER_VS_OURS_M5.md authoring + smokes), M1 (Jose-owned housekeeping). Lint rule updated: [`scripts/wiki_lint.py`](../scripts/wiki_lint.py) `ROOT_BASENAMES` now treats `TODO.md` as a deliberate root page (orphan-exempt), parallel to `log.md` and `README.md`.
- Backfill: 2026-05-09 commit `e9895b1` ("docs(m5): M5/M5.1 plan + M4 smoke iteration log + orchestrator") added five artifacts; the previous-day log entry covered three of the five (M5 docs trail) but missed two. Logging now: created [`report/RESULTS_SMOKE_m4.md`](report/RESULTS_SMOKE_m4.md) (live record of the M4 smoke iterations on Qwen3.5-0.8B hybrid; v1 placeholder M4.1 → v2 canonical multi-block tool-response → v3 per-chunk cap + 8192 input + tightened prompt; mean EM stayed below 0.01 across all three, motivating the qwen35_minimal pivot decision recorded in M4.2). Created [`scripts/orchestrate_C_then_A.sh`](../scripts/orchestrate_C_then_A.sh): autonomous M4 orchestrator running option C (4 prompt configs × 7 datasets × n=1000) followed by option A (locked-best per-variant defaults × full data, both variants); 6 resumable phases with `START_PHASE` env var; locked-best mappings (hybrid → `qwen35_minimal`; base → `qwen35_minimal_no_system`) sourced from §6-§7 of [`report/RESULTS_SMOKE_m4.md`](report/RESULTS_SMOKE_m4.md). Created [`scripts/_finish_m4_hybrid.sh`](../scripts/_finish_m4_hybrid.sh) (was untracked in working tree, now committed): one-shot wrapper that boots the 16-worker retriever, waits up to 90×5 s for `/health`, then invokes `orchestrate_C_then_A.sh` from `START_PHASE=6` (the hybrid-A full-data tail). Created [`scripts/_finish_m4_two_cells.sh`](../scripts/_finish_m4_two_cells.sh) (also previously untracked, now committed): pre-flights retriever + SGLang `/health`, cleans empty result dirs from earlier failed attempts (2026-05-10 14:35 timestamp prefix), then runs the two missing hybrid FULL cells (2wikimultihopqa, musique) via [`scripts/run_m4.sh`](../scripts/run_m4.sh) with `PROMPT_MODE=qwen35_minimal`. Decision: from now on, every artifact added in a commit must have a corresponding `log.md` bullet in the same commit; the gap-fill above is the last retrospective entry of this kind.

## 2026-05-09

- Decision: launched **M5** — Qwen3.5-0.8B GRPO training on the existing NeMo-RL scaffold ([`training/`](../training/)), with M5.1 sub-phase aligning all knobs to the [ReSearch paper](https://arxiv.org/abs/2503.19470) recipe + [`Agent-RL/ReSearch`](https://github.com/Agent-RL/ReSearch) configs (modulo two intentional divergences: F1-only reward on `<answer>…</answer>`, no `\boxed{}` wrapper). Verl was rejected (CLAUDE.md gotcha: no Qwen3.5 support); recipe ports knob-by-knob into NeMo-RL instead. Train rollout is byte-aligned to the [M4 eval pipeline](milestone_4/MILESTONE_4.md) so the trained checkpoint is directly evaluable without re-aligning (avoids the M3-style 14-fix train/eval-drift audit by construction). Training data = MuSiQue (single-dataset; multi-hop, hardest of the four paper benchmarks; largest single-dataset headroom in M1 numbers at EM 0.124). Folder layout: each experiment is a fully self-contained copy at the repo root (`training_m5_1/`, `training_m5_2/`, …) so a long M5.1 full-training run can run uninterrupted while M5.2 is being configured + smoked on a separate box. Each experiment ships its own `nemo_rl/` (vendored), `src/` overlay, `configs/`, `scripts/`, `setup.sh`. Doc trail: [`milestone_5/MILESTONE_5.md`](milestone_5/MILESTONE_5.md), [`report/CODE_SETUP_m5.md`](report/CODE_SETUP_m5.md), [`report/RESULTS_SMOKE_m5.md`](report/RESULTS_SMOKE_m5.md) (all three are spine-only at creation time; populate as the smokes run). [`milestone_5/PAPER_VS_OURS_M5.md`](milestone_5/PAPER_VS_OURS_M5.md) is the planned clause-by-clause paper-to-YAML mapping (TODO, blocks the M5.1 config).
- Decision: rewrote [`docs/vast/SETUP_VAST.md`](vast/SETUP_VAST.md) to be the canonical Vast setup guide and extended [`training/scripts/bootstrap.sh`](../training/scripts/bootstrap.sh) so one command provisions the full box: corpus + IVF-SQ8 index + e5-base-v2 encoder (mirrors `local_retriever/README.md` download steps) + Qwen3.5-0.8B hybrid + base for M4 eval. M1-era walkthrough archived to [`docs/archive/VAST_INSTANCE_SETUP_v0.md`](archive/VAST_INSTANCE_SETUP_v0.md) (manual `huggingface-cli download` flow + flat IP index + GRPO checkpoint downloads — superseded). Flat IP index is no longer used. [`scripts/m4_download_models.sh`](../scripts/m4_download_models.sh) now auto-discovers a Python interpreter (Vast → ALICE → system fallback) so it works on either host. Commits `0135d8e` (script changes) + `b99f19f` (per-scenario disk budget table; user-edited).
- Decision: archived the second M1-era manual walkthrough [`docs/setup/BOOTSTRAP_NEW_INSTANCE.md`](setup/BOOTSTRAP_NEW_INSTANCE.md) to [`docs/archive/BOOTSTRAP_NEW_INSTANCE_v0.md`](archive/BOOTSTRAP_NEW_INSTANCE_v0.md) (parallel content to VAST_INSTANCE_SETUP_v0). Replaced with a thin host-agnostic doc at the same path: `docker pull`/`run` recipe, the `--gpus all` + `--shm-size=16g` + bind-mount + port-mapping flags that aren't in SETUP_VAST.md, the `/venv/...` symlink caveat for non-Vast hosts, then delegates the actual setup steps (asset downloads, venvs, retriever start) to SETUP_VAST.md and `bootstrap.sh`. Fixed the 4 incoming "Vast bootstrap" links across `docs/TODO_2026-05-04.md`, `claude/CLAUDE.md` (symlinked from `.claude/CLAUDE.md`), and `docs/report/CODE_SETUP_m2.md` to point at SETUP_VAST.md (which is what they actually meant). The `docs/setup/` slot now serves as the non-Vast/lab-cluster wrapper, SETUP_VAST.md is the Vast canonical doc, and both go through the same `bootstrap.sh`.
- Decision: **closed the project history through M3** with a milestone-numbered reorg of `docs/report/`. v0/v1/v2/v3-suffixed reports renamed to m0/m1/m2/m3/m4 (CODE_SETUP_v0 → m0, RESULTS_v0/v1 → RESULTS_m0_a / m0_b, FROZEN_CONFIG_v1 → CODE_SETUP_m1, RESULTS_PLAN_B + COMPARISON_PLAN_B_v1 merged → RESULTS_m1, COMPARISON_PLAN_B (Plan-B v0 pre-fix) archived to docs/archive/COMPARISON_PLAN_B_v0.md, CODE_SETUP_v1/v2/v3 → m2/m3/m4, RESULTS_v2/v3 → m3/m4). Milestone dirs renamed: `docs/milestone_one/two/three/four/` → `milestone_1/2/3/4/`. Asset dirs `results_v0_assets/` and `results_v1_assets/` moved under `docs/report/archive/m0_a/` and `m0_b/`. Pulled all M3 + M3.1 per-(variant, dataset) `metric_score.txt` from ALICE (`s4374886@login.alice.universiteitleiden.nl:/home/.../evaluation_research/results/`) into `docs/report/archive/m3/{pre_grpo,post_grpo_v0,post_grpo_v0_no_ex}/<dataset>/metric_score.txt` (3 × 7 = 21 files); curated the M1 metric mirror at `archive/m1/{base,instruct}/<dataset>/` (14 files copied from `evaluation_search_r1/results/_archive_v1/`). Repo is now self-contained for fact-check.
- Harmonised all 10 `CODE_SETUP_m*.md` and `RESULTS_m*.md` files to a coherent template (frontmatter + 4-line preamble, §1 Headline diff, §2 What's unchanged, §3 Critical bugs/changes, §N Pointers); CODE_SETUP_m1 was fully restructured (was FROZEN_CONFIG, now 8-section spine with the audit-fix table integrated) and RESULTS_m1 was rewritten from a stitched concat into the 10-section RESULTS template.
- Two known content errors propagated through the m0 docs and the culmination docs (PROGRESS_REPORT_01_m0_to_3 + SUPERVISOR_MEETING_2026-05-07_m0_to_3): (1) Phase-1's reward is the **paper-faithful ReSearch reward (format + EM partial credit)**, not Search-R1's EM-only `qa_em.py`; (2) Phase-1 builds on the **ReSearch paper only**, not Search-R1, and the `<search>` / `<result>` tag scheme is ReSearch's (the supervisor brief had been fixed in commit `caada5f`; the progress report had not).
- Fact-check pass: cross-checked every numerical claim in the harmonised reports against ground-truth `metric_score.txt` + W&B CSVs (M0_a 14 runs first/last decile, M0_b 15 runs, M1 7×2 cells + grand averages, M3 + M3.1 7×3 cells + simple-means + per-dataset deltas; wall-clock claims against `docs/training/SMOKE_RESULTS_2026-05-06.md` line 1716 / 1754 and `docs/log.md` line 38). **0 numerical deltas found.** Step counts also verified: z7kcxfof 1046 (claim matches CSV last `_step`); el6s2d2h 2280 (CSV last `_step` = 2279; the +1 is the 1-indexed convention in the doc claim). HF checkpoint sha256 in CODE_SETUP_m1 flagged as out-of-band (model dirs are empty locally per inventory).
- Cross-doc link sweep: rewrote ~216 references across ~50 tracked files (`claude/CLAUDE.md`, `docs/log.md`, milestone narratives, conversation contexts, papers, scripts, README files) to the new m<N> names. PROGRESS_REPORT_01.md → PROGRESS_REPORT_01_m0_to_3.md and SUPERVISOR_MEETING_2026-05-07.md → SUPERVISOR_MEETING_2026-05-07_m0_to_3.md propagated everywhere.

## 2026-05-08

- Decision: launched **M4** — Qwen3.5-0.8B baseline eval pipeline. Stand up [`evaluation_qwen35/`](../evaluation_qwen35/) as a copy of [`evaluation_research/`](../evaluation_research/) with a new `prompt_mode=qwen35` that swaps Search-R1's invented `<search>` / `<result>` for Qwen3.5-native `<tool_call>` / `<tool_response>` (ids 248058/248059, 248066/248067; vocab tokens but not "special"). Same prompt prose as the M3 `p1_basic_w_ex` template. Surface area mirrors the M3 14-fix audit — six files: `templates.py` (+ `QWEN35_0_8B_TEMPLATE` / `QWEN35_TEMPLATES`), `parser.py` (+ `extract_tool_call_query`), `active_pipeline.py` (family-based dispatch: qwen3 / qwen35 / search_r1), `utils/utils.py` (`get_generator` short-circuits on `framework=sgl_remote` before the multimodal-detection check; Qwen3.5 has `vision_config` in config.json so the existing check would mis-route to `HFMultiModalGenerator`), `run_eval.py` (+ `qwen35*` modes; `--test_sample_num` / `--random_sample` / `--seed` for the 100-item quick eval, deterministic via `random.seed(config[seed])`), `setup.py` (distribution name `evaluation-qwen35`). Models downloaded on ALICE: [`Qwen/Qwen3.5-0.8B`](https://huggingface.co/Qwen/Qwen3.5-0.8B) (hybrid, 1.7 GB) + [`Qwen/Qwen3.5-0.8B-Base`](https://huggingface.co/Qwen/Qwen3.5-0.8B-Base) (base, 1.7 GB) at `eval/qwen3.5_0.8b/` and `eval/qwen3.5_0.8b_base/`. First smoke (100 items / dataset, 7 datasets, hybrid) submitted as sbatch **2150578** on `gpu-short` (1 h cap). Doc trail: [`milestone_4/MILESTONE_4.md`](milestone_4/MILESTONE_4.md), commits f91325d + 2be8e0a on `research_v2`.
- Decision: launched **M3.1** — second Phase-1 checkpoint evaluation. Target: `p3_decide_no_ex_el6s2d2h` (Phase-1 v0 highest-reward run: end reward 0.215 / +43 % rel over 2280 steps; vs M3's z7kcxfof at 0.190 / +28 % rel over 1046 steps). Settles whether the +0.025 rollout-reward gap translates to held-out EM, or is mostly the partial-credit-floor artifact. Sbatch job **2134645** queued (gpu-short, 4 h, ALICE A100-80GB). Setup is additive-only (no new alignment fixes; the M3 14-fix audit holds): added `P3_DECIDE_NO_EX_TEMPLATE` to `evaluation_research/flashrag/search_r1/templates.py` (verbatim from training, recovered from `report/RESULTS_m0_a.md` §`p3_decide_no_ex (el6s2d2h)`); added `QWEN3_TEMPLATES` registry keyed on `prompt_mode`; switched `prompt_mode == 'qwen3'` checks in `active_pipeline.py` and `run_eval.py` to `prompt_mode.startswith('qwen3')` so qwen3 family shares retrieval format / budgets / `enable_thinking`. Checkpoint converted verl-FSDP → HF via `python -m verl.model_merger merge --backend fsdp …` (1.5 GB safetensors at `eval/qwen_3_0.6b_v0_no_ex/`). Doc trail: [`milestone_3/MILESTONE_3.1.md`](milestone_3/MILESTONE_3.1.md), [`report/CODE_SETUP_m3.md`](report/CODE_SETUP_m3.md) §13.5, [`report/RESULTS_m3.md`](report/RESULTS_m3.md) §14, [`report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md`](report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md) §4 (parallel M3+M3.1), [`report/PROGRESS_REPORT_01_m0_to_3.md`](report/PROGRESS_REPORT_01_m0_to_3.md).
- Decision: bumped SGLang `/health`-readiness wait in `scripts/sbatch_m3.sh` from 300 s to 600 s after sbatch **2134645** failed at the cliff (M3 reference: 260 s; this run: cold cache on `node875`, would have needed ~310 s). The retriever + checkpoint + pipeline were all healthy at the failure point; only the wait window was too tight. Re-submitted as sbatch **2134663** (same partition, same flags, only the wait window bumped). 600 s is the same budget the retriever wait already uses, so the two waits are now symmetric.
- Decision: published the M3.1 checkpoint to HuggingFace as [`pantomiman/Qwen3-0.6B-v0.1`](https://huggingface.co/pantomiman/Qwen3-0.6B-v0.1) — parallel to the M3 checkpoint at [`pantomiman/Qwen3-0.6B-v0`](https://huggingface.co/pantomiman/Qwen3-0.6B-v0); the `.1` minor-version suffix distinguishes the no-example variant trained from the same base + algorithm + reward + data, varying only the prompt.
- Decision: **M3.1 closed**. After two infra-wait failures (sbatch 2134645 SGLang `/health` 300 s cliff → bumped to 600 s; sbatch 2134663 retriever `/health` 600 s cliff → bumped to 1200 s), **sbatch 2150167** completed all 7 datasets cleanly in 1 h 32 m on `node870` (well under the 2.5 h M3 reference). Result: simple-mean EM **0.169** (M3 was 0.155, pre-GRPO 0.102) — +0.014 abs / +9 % rel over M3, +0.067 / +66 % over pre-GRPO. 5 / 7 datasets beat M3 (PopQA +0.058, TriviaQA +0.037, HotpotQA +0.028 the largest); 1 tie (2Wiki); 1 small-N regression (bamboogle −0.032 on N = 125, edge of significance). ACC / F1 widen the M3.1-vs-M3 gap to +12 % / +14 %, indicating the no-example variant produces higher-quality answers that don't always pass EM strict-match. **Conclusion**: the Phase-1 structural finding (decision-rule scaffolding can substitute for the few-shot example, no-example variant has more headroom) holds under held-out evaluation — the no-example + decision-rules prompt is a real lever, not a partial-credit-floor illusion. Recipe transfer to Qwen3.5 (Phase-2) is lower-risk: the prompt-design lever is established at 0.6B, and Phase-2 can ablate it on 0.8B / 2B without re-discovering it. Numerical record: [`report/RESULTS_m3.md`](report/RESULTS_m3.md) §14; supervisor brief: [`report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md`](report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md) §4 (parallel M3+M3.1; §0 TL;DR §1 also covers both).
- Built training-curve comparison panel for the with-example (z7kcxfof) vs no-example (el6s2d2h) Phase-1 v0 runs. Source: archived W&B histories at `report/archive/m0_a/csv/{p1_basic_w_ex_z7kcxfof,p3_decide_no_ex_el6s2d2h}.csv`; 4-panel grid (reward / tool calls / num turns / response length) overlaid with SMA window 20; output at `report/archive/m0_a/comparison_z7kcxfof_vs_el6s2d2h.png`; reproducer at [`scripts/plot_m3_1_panel.py`](../scripts/plot_m3_1_panel.py). The panel makes the structural divergence glanceable: same base + algorithm + reward + data, only the prompt differs — yet z7kcxfof anchors at heavy-tool 2/4/~2050-tok and el6s2d2h converges to standard 1/3/~1100-tok at slightly higher final reward. Embedded in `milestone_3/MILESTONE_3.1.md` (deliverable 10), `report/RESULTS_m3.md` §14.7, and `report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md` §4.7. M3.1 deliverable list now fully closed.

## 2026-05-07

- Decision: M3 closed. First evaluation of the v0 GRPO checkpoint (`p1_basic_w_ex_z7kcxfof`, 1046 / 9968 steps, 23h 47m 30s wall on 1× A100-40GB) vs. the untrained Qwen3-0.6B hybrid on the 7 paper QA benchmarks. Headline: average EM 0.102 → 0.155 (+0.053 absolute, +52 % relative) across full Plan A (51,713 items / variant); 6 / 7 datasets improved; held-out generalisation rules out memorisation. The eval pipeline is now pinned and reusable for Phase-2 NeMo-RL evaluation. Full record: [`report/RESULTS_m3.md`](report/RESULTS_m3.md); consolidated brief: [`report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md`](report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md) § 4; closed milestone: [`milestone_3/MILESTONE_3.md`](milestone_3/MILESTONE_3.md).
- Decision: ReSearch action-format attribution corrected. The published ReSearch paper uses `<search>` / `<result>` tags (verified upstream `re-search` commit `51d98e1`); the `<tool_call>` JSON variant in our local `re-search/` checkout is a separate ablation we introduced (commit `2c32dd3`, 2026-04-12) and tested in the v1 block. Earlier docs that called `<tool_call>` "ReSearch's format" were misattributing our own ablation. Fixed in `report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md` § 4.5 and `report/RESULTS_m3.md` § 10.7.
- Decision: heavy rebuild of `report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md` into a 6-section canonical chapter-closing summary (§ 0 TL;DR · § 1 Phase-1 · § 2 M1 baseline · § 3 M2 NeMo-RL pivot · § 4 M3 results · § 5 compute reality + reframed RQ · § 6 next-steps recipe). Cross-references in downstream docs bumped from old § 1 / § 2 / § 3 to the new section numbers.
- Created: [`research/QUESTIONS.md`](research/QUESTIONS.md), running register for open research questions and "register this as a question" entries. Threaded into [`../claude/CLAUDE.md`](../claude/CLAUDE.md) (repo layout + new "register a question" rule under "How I want you to work with me"), [`research/README.md`](research/README.md) (intro + doc inventory), and [`research/CONVERSATION_CONTEXT.md`](research/CONVERSATION_CONTEXT.md) (doc inventory). First question filed: Q1 sequence packing in verl and NeMo-RL, works with Qwen3 but not Qwen3.5.

## 2026-05-06

- Added: E2H curriculum recipe entry in [`research/RECIPES.md`](research/RECIPES.md) (status: planned, item #5 in active ablation list). Lays out the data-side curriculum mechanics, hop-count difficulty mapping for retrieval QA (NQ=0/HotpotQA=1/MuSiQue=2), borrow-worthy tricks in priority order (hop-count bucketing → stage-based fade → Gaussian schedule), and the JustRL counter-evidence caveat that the C-minimal control must validate against. No runs yet.
- Ingested: https://arxiv.org/abs/2506.06632 (E2H Reasoner, ICLR 2026) -> [`papers/2506.06632_e2h.md`](papers/2506.06632_e2h.md) (deep ingest), [`research/DATASET_ANALYSIS.md`](research/DATASET_ANALYSIS.md) (curriculum application section). PDF capture pending; should land at `raw/papers/2026-05-06_e2h.pdf`. Existing brief card in [`research/SURVEY_FOCUSED.md §5.4`](research/SURVEY_FOCUSED.md) left in place.
- Created: [`research/DATASET_ANALYSIS.md`](research/DATASET_ANALYSIS.md) - covers all 7 Search-R1 benchmarks (NQ, TriviaQA, PopQA, HotpotQA, 2WikiMultiHopQA, MuSiQue, Bamboogle) with hops, sizes, structure, real example pulled from our `data/<dataset>/test.jsonl`, and v1 base EM vs paper. Maps E2H difficulty ordinals to NQ (0) / HotpotQA (1) / MuSiQue (2); the other 4 stay held-out OOD.
- Learned: E2H paper does **not** evaluate on retrieval-augmented multi-hop QA (only closed-book MATH / Countdown / Blocksworld). Applying it to our setting is a thesis novelty; convergence rates may differ from paper's pp gains. Filed in [`papers/2506.06632_e2h.md`](papers/2506.06632_e2h.md) "Limitations" + "Takeaways for us".
- Created: [`research/RECIPES.md`](research/RECIPES.md) - living catalog of tested and referenced recipes with analysis, bad/good breakdown, our results with actual run numbers, and 8 cross-cutting learnings (L1-L8) grounded in the v0/v1/smoke result files.
- Synthesised: [`research/RECIPE_COMPARISON.md`](research/RECIPE_COMPARISON.md) - grounded side-by-side of all three RL+search papers (Search-R1, R1-Searcher, ReSearch). Every row sourced from the paper or scripts; rows we could not verify are marked "not stated" or "not in our extract".
- Ingested: [`raw/papers/2026-05-06_search-r1.pdf`](raw/papers/2026-05-06_search-r1.pdf) (Search-R1, arXiv 2503.09516v5) -> [`papers/2503.09516_search-r1.md`](papers/2503.09516_search-r1.md). This is our M1 reproduction target (Plan B v1 within ±2.5 pp paper avg, see [`report/RESULTS_m1.md`](report/RESULTS_m1.md)). Stated compute: 8 H100, single node, 500 steps; wall-clock not stated. Headline GRPO group size G not in our extract (ablated 1/3/5 in Appendix H).
- Decision: enforce the no-inference rule on paper notes. Paper-note fields that are not stated are now marked "not stated" rather than inferred. Strengthened in [`papers/README.md`](papers/README.md) under "No inference rule"; existing R1-Searcher and ReSearch notes scrubbed of GPU-type and wall-clock guesses; ReSearch's "0.46 EM at 7B" mistake corrected (that figure is the 32B-Instruct row).
- Ingested: [`raw/papers/2026-05-06_r1-searcher.pdf`](raw/papers/2026-05-06_r1-searcher.pdf) (R1-Searcher, arXiv 2503.05592v2) -> [`papers/2503.05592_r1-searcher.md`](papers/2503.05592_r1-searcher.md). Headline takeaways: 2-stage curriculum + asymmetric `+0/-2` format penalty are the borrow-able tricks; G=16 / 29k generate-max not affordable at our scale (8 GPUs, type not stated).
- Ingested: [`raw/papers/2026-05-06_research-rl.pdf`](raw/papers/2026-05-06_research-rl.pdf) (ReSearch, arXiv 2503.19470v3, NeurIPS 2025) -> [`papers/2503.19470_research.md`](papers/2503.19470_research.md). This is the recipe behind our [v0](report/RESULTS_m0_a.md) and [v1](report/RESULTS_m0_b.md) Phase-1 Qwen3-0.6B results. 64×H800 paper config; the 0.1 partial-credit reward is the most ablation-worthy line in the loss and the paper does not ablate it.
- Decision: stand up [`papers/`](papers/) as a wiki section for deep, single-paper notes (companion to `research/SURVEY.md`). Convention captured in [`papers/README.md`](papers/README.md); template at [`templates/paper.md`](templates/paper.md).
- Decision: adopt Karpathy's wiki workflow on top of `docs/`. Recorded in [`SCHEMA.md`](SCHEMA.md), pointer added to [`../claude/CLAUDE.md`](../claude/CLAUDE.md).
- Ingested: [Karpathy gist on personal wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) -> [`SCHEMA.md`](SCHEMA.md), [`raw/README.md`](raw/README.md), this file.
- Lint: post-papers-ingest run, 96 files total. Same 3 pre-existing broken links as before (none introduced by paper ingestion); orphans = 8 sankar/* (CV strand, expected) + 2 templates/* (`daily.md`, `zettel.md`; invoked by Templater plugin, not linked). New files [`papers/README.md`](papers/README.md), [`papers/2503.05592_r1-searcher.md`](papers/2503.05592_r1-searcher.md), [`papers/2503.19470_research.md`](papers/2503.19470_research.md), [`templates/paper.md`](templates/paper.md) all link-resolve cleanly.
- Lint: first lint cycle run. 3 pre-existing broken links flagged (not introduced by wiki conversion): `archive/training/SMOKE_RESULTS_2026-05-02_smoke_shape.md` -> `local_retriever/README_v1.md`; `milestone_1/MILESTONE_1.1_QWEN_BASELINES.md` -> `RESULTS_QWEN3_BASELINE.md` (file not yet authored, Jose-owned task); `retriever/INDEX_ARCHITECTURE.md` -> `../../../index_creation/README.md` (assumes `/workspace/...` host path). 8 sankar/* orphans (CV strand, expected; only some are linked from `sankar/00_CONVERSATION_CONTEXT.md`). 1 frontmatter-incomplete (`sankar/03_RESUME_LEGACY.md` has Obsidian-style frontmatter with different keys).
- Learned: paper batch shape is 2560 traj/step (512 prompts × 5 gen + 10 grad updates); ours is 510 traj/step (1/5th paper); per-step time is rollout-dominated, so paper config on 1× A100 is ~5× slower per step (~55-85 d / run). Filed in [`training/PAPER_VS_OURS_TRAINING.md`](training/PAPER_VS_OURS_TRAINING.md), [`report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md`](report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md), [`report/PROGRESS_REPORT_01_m0_to_3.md`](report/PROGRESS_REPORT_01_m0_to_3.md).

## 2026-05-05

- Decision: pivot the thesis question from "extend RLVR via tool-use" to "is post-training a small LM to Search-R1 results feasible under realistic resource constraints, and what is the optimised recipe?" Recorded in [`report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md`](report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md).
- Decision: candidate recipe = E2H curriculum + S-GRPO + MC-GRPO with a JustRL plain-GRPO control. Recorded in [`report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md`](report/SUPERVISOR_MEETING_2026-05-07_m0_to_3.md), expanded in [`TODO_2026-05-04.md`](todo/TODO_2026-05-04.md).

## 2026-05-04

- Learned: Phase-1 Qwen3-0.6B partial-credit reward creates a 0.1 floor that masks the tool-use signal (3-6 pp gap between tool-using and no-tool runs). Filed in [`report/RESULTS_m0_a.md`](report/RESULTS_m0_a.md), [`report/RESULTS_m0_b.md`](report/RESULTS_m0_b.md).
- Learned: Qwen3-0.6B base model cannot bootstrap the tool-call format from cold-start (5/5 v1 base attempts stayed at 0 tool calls, longest 2300 steps). Filed in [`report/RESULTS_m0_b.md`](report/RESULTS_m0_b.md). Decision: don't retry without SFT warm-start.
